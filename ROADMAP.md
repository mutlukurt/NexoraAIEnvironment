# NexoraAI Roadmap

> **Historical document.** This file records plans and implementation claims from an
> earlier development stage. See [ROADMAP-2026.md](ROADMAP-2026.md) and
> [docs/CURRENT-TRUTH.md](docs/CURRENT-TRUTH.md); current source and evidence win.

The plan for going from **1 to 10**: first make the engine faster on the same hardware, then make small models act like masters, then give the agent eyes, and finally widen the platform. Each item is checked off as it ships.

**Guiding principle** (same one that built this app): don't ask the model nicely — make failure impossible, verify everything deterministically, and hide model mistakes from the user.

---

## Phase 1 — The Engine (v0.10.x)

Speed and context wins that benefit every user, on the exact same models.

- [x] **1.1 Flash attention + KV cache quantization** — enable `flashAttention` and a quantized KV cache in the inference worker; roughly doubles the usable context window in the same RAM. Cheapest win first. *Shipped: the worker now tries flash attention + Q8_0 KV first (context tiers doubled) and falls back to the old F16 path per size on unsupported models/hardware. Verified live: a Qwen2.5-Coder-3B that previously got a 16k window now loads with its full 32k train context on the same 16 GB laptop.*
- [x] **1.2 Partial GPU offload** — a GPU-layer slider plus VRAM-aware auto-recommendation in the Hardware Advisor. Small VRAM cards (e.g. 4 GB RTX 2050) currently fall back to pure CPU; offloading half the layers can nearly double speed. *Shipped: the worker now offloads layers with a step-down ladder (requested/auto → 60% → 40% → 20% of the model's layers → CPU), because Vulkan's free-VRAM estimate is optimistic and even `auto` can OOM while loading weights. Settings gained a GPU-layer slider (0 = auto-fit to free VRAM) and the model card shows the real offload ("GPU 16/29"). Measured on the 4 GB RTX 2050 dev laptop with Qwen2.5-Coder-7B: 4.2 → 7.2 tok/s (+71%), context 16k → 32k.*
- [x] **1.3 Per-phase sampler presets** — plan/enhance = 0.7 temperature (creativity), code generation = 0.2, "düzelt" fix = 0.05–0.1 (surgeon's hand). Three-line change, measurable quality. *Shipped: the single `chat.send` site in the app store now derives sampling from the turn's phase flags — plan/enhance turns get 0.7/topP 0.95, code stays at 0.2, and every fix turn (including the automatic re-tries) runs at 0.1.*
- [x] **1.4 Context compaction with summary** — before the 75% reset, have the model write a 3-sentence summary and carry it into the fresh session, so "what the user accepted/rejected" survives compaction. *Shipped: when the window passes 75%, the worker first asks the still-alive session for a 3–5 sentence summary (never streamed to the UI, capped at 250 tokens, skipped when no headroom remains) and embeds it in the fresh session's compaction note; on any failure it falls back to the old summary-less note. Verified live: a planted fact ("Fırın Luna", purple theme) survived a real 12.5k/16k compaction and was recalled correctly afterwards.*
- [x] **1.5 Migrate main inference to `llama-server`** — the app already ships llama.cpp's official server for vision. Moving chat inference to it unlocks prompt caching / KV reuse (iterations stop re-prefilling the whole project every turn — the single biggest latency cost on CPU), parallel slots (vision + code at once), and speculative decoding (0.5B draft + 7B main, 30–60% faster on code). *Shipped: chat inference now runs through llama-server (auto-downloaded, GPU build preferred — Vulkan on Linux/Windows, Metal on macOS) with flash attention + Q8_0 KV, `--cache-reuse`, `-ngl auto` and a spawn ladder; chat history lives in the main process and shared prefixes hit the server's prompt cache (measured: a 7.7k-token project prefix re-prefilled in 0.14s instead of 23.5s — ~170×). The CJK ban became a per-request logit_bias (token IDs extracted by a 1-second vocab-only scan, cached per model), compaction-with-summary moved up with the history, and the node-llama-cpp worker remains as an automatic fallback engine — verified live by making the server binary unexecutable.*

## Phase 2 — Making Small Models Masters (v0.11.x)

A small model's weakness isn't intelligence, it's freedom. The less freedom, the more mastery.

- [x] **2.1 GBNF grammar-enforced edit blocks** — constrain output at the sampler level so the SEARCH/REPLACE format *cannot* be violated. Retires the streaming watchdog and corrective retries; the same philosophy as the CJK token ban. *Shipped: UPDATE turns now run under a GBNF grammar — SEARCH is hard-capped at 12 lines (verified live: a 3B trying to copy the whole file was cut at exactly line 12 and forced into REPLACE), REPLACE at 40, edit targets are enumerated from the real project paths so a nonexistent file cannot be edited, new-file/[DELETE]/agent-directive shapes stay legal, and pure questions get a bounded `ANSWER:` escape (backtick-free, ≤8 lines) that the chat strips before display. Two grammar holes were found by testing and closed (a `lang="edit"` ambiguity that let the model skip the SEARCH marker, and block-shaped lines leaking into the ANSWER escape); a 59-optional character chain was rewritten after it blew up llama.cpp's grammar-state tracking. A malformed grammar can never brick a turn: if the server rejects the request before any token streams, it is retried without the grammar. The renderer watchdog stays as the safety net for the worker fallback engine. End-to-end in Electron: "rename the heading" produced a single 1-line SEARCH block through the full app path.*
- [x] **2.2 Plan → file-by-file generation** — for big requests: approved plan, then one fresh-context prompt per file ("write only `Hero.tsx`; here are `data.ts`'s types"). Small models are consistent within one file — this generalizes that insight to multi-file projects, with per-file retry. *Shipped: plan turns now emit a grammar-enforced `N. path — description` file list; approving it starts an orchestrated build — files are deterministically reordered (css/lib foundations → ui → sections → entry last) and each is generated in its own short turn, grammar-locked to exactly one fenced block for exactly that path, with lib/css contracts embedded, one retry per missing file, auto-apply per file (the plan approval was the consent; undo stays available) and a final build report. Previous files ride along in the engine history, which the server's prompt cache makes nearly free. Verified live with a 3B: an 8-file cafe-site plan parsed cleanly and per-file turns produced single clean blocks in 6–14s each. The hard-won lesson: a grammar must allow EOS immediately after the closing fence — a mandatory trailing newline masks EOS and sends the model into an `[END_OF_TEXT]` spiral until the token cap; plan-line paths also need parser-side sanity filters (length/depth/text-extension) against in-path repetition spirals. If no file list can be parsed (e.g. worker fallback engine), the old single-shot build runs unchanged.*
- [x] **2.3 Auto-verify after every generation** — run the existing background build check after *every* generation (not just Run), silently auto-fix on failure, then announce "generated *and* compiled". The user never sees broken output. *Shipped as two layers: an instant Babel syntax check of the generated files (milliseconds, no node_modules needed — catches the unterminated-quote/brace/JSX class that dominated the real 14B campaign, verified against broken TSX/TS fixtures with exact line/col output) plus the full vite build check when the project's node_modules already exists (a fresh project never triggers a silent minutes-long npm install — full compile still happens at Run). Failures feed up to two hidden "düzelt" rounds — no user bubble, no technical prompt — announcing only when something was found ("sessizce düzeltiliyor"), fixed ("otomatik giderildi — çıktı doğrulandı") or given up on; clean generations stay silent. Planned builds verify once at the end of the sequence rather than per file, and the loop is guarded against the Run-triggered fix flow, user-initiated turns and IPC settle races.*
- [x] **2.4 Section template bank** — 10–15 hand-crafted, design-proven parametric section templates (Hero, Pricing, FAQ, Footer…). The model fills in content and theme instead of writing from scratch: 3B + good template beats 14B + blank page. *Shipped: 11 premium Tailwind skeletons (hero, navbar, features, gallery/menu grid, pricing, testimonials, FAQ, CTA, contact, footer, about) matched by TR+EN keywords on the planned file's path and description (order-sensitive: hero beats navbar's "header", a priced food menu beats the pricing table) and embedded into that file's planned-build prompt. The decisive lesson came from live testing: a 3B copied natural-sentence placeholders verbatim while happily adapting the color theme — small models follow the example harder than the instruction. Rewriting every placeholder as an explicit `{{MARKER}}` flipped the result completely: brand content in, amber theme in, structure kept, zero markers left, 16s on the 3B. A deterministic retry catches any file still containing unfilled `{{ }}`.*
- [x] **2.5 Model-family prompt profiles** — detect the family (Qwen/Gemma/Llama) from GGUF metadata and apply family-specific quirk fixes, the second axis next to size-adaptive prompting. *Shipped: `detectFamily` classifies qwen/gemma/llama/deepseek/phi/mistral/generic from architecture + name + filename; a short family note (output-discipline / preamble suppression for the chattier families, none for the Qwen baseline) threads through the engines into the system prompt. Testing on a real Gemma surfaced and fixed two bugs: reasoning models returned empty output (fixed by disabling thinking via `chat_template_kwargs`), and — caught by the owner — conversational messages triggered the whole build pipeline (fixed with a `looksLikeBuildRequest` gate so a greeting gets a plain reply while a real build request still enhances/plans). Unit tests 10/10 (family) + 20/20 (build-intent); both fixes GUI-verified end to end.*

## Phase 3 — An Agent With Eyes (v0.12.x)

- [x] **3.1 Import existing projects** — "Open folder" brings any real project into the workspace; smart context and surgical edits already work. Turns the app from a site generator into a local coding assistant for every React project. *Shipped: sidebar "Klasör Aç" imports and LINKS a real folder — a safe scan (build dirs skipped, text-only, capped) loads the files, and a linked-folder registry redirects the whole workspace (edits, Run, dev server, build check) to the original folder, writing only changed files and never scaffolding over a real project. Live-verified: an external Vite+React project was imported and a 3B's surgical edit landed in the original folder's git diff. The test also caught and fixed two real bugs: plan mode hijacking small iteration requests into invented rebuild plans, and the 3.2 runtime hook POSTing to a raw \${RUNTIME_ERROR_PORT} placeholder URL (self-healing had been silently dead).*
- [x] **3.2 Runtime error capture** — inject a tiny error hook into the dev-server page (`window.onerror` + console.error → local endpoint → chat), so "düzelt" covers runtime errors, not just compile errors. *Shipped beyond the original scope: the hook (injected into the project's index.html at sync time, invisible in the code view) posts uncaught errors and rejections to a local collector, and the app starts a hidden fix turn AUTOMATICALLY — zero human input, two attempts per error signature, honest hand-back after that. Stack URLs are rewritten to project paths so smart context targets the right file. Born from the live-test finding that a missing `useState` import compiles cleanly and dies only in the browser; verified end to end with a synthetic error → capture message → automatic fix turn in four seconds.*
- [x] **3.3 Visual self-review** — after Run, screenshot the page and show it to the app's own vision model: empty sections? overflow? does it match the reference image? Auto-iterate if not. The app can already see — it just doesn't look at its own work yet. *Shipped with a deterministic twist: the dev page is captured offscreen and FIRST measured by a pixel scan (dominant-color ratio ≥0.98 = blank page — live testing showed the VL-3B answering 'OK.' to an all-white page, so blankness is measured, not asked), THEN reviewed by the vision model for concrete defects when it's already on disk. Defects start a hidden fix turn feeding the new autonomous repair ladder (rung 0: model-free deterministic fixes for missing imports/apostrophe strings/broken import paths; rung 1: line-numbered context + no-op turns escalate instead of burning retries; rung 3: verify-clean versions are tagged green and the project is auto-restored to the last working version when all else fails). Zero-touch proof: an imported project with a deliberate missing-useState import was detected from the live page and repaired on disk with NO model loaded.*
- [x] **3.4 Git-based generation history** — every accepted generation becomes a commit; unlimited timeline instead of the 20-step undo buffer. *Shipped: post-verify auto-commits each generation (visible request as subject), a Geçmiş tab lists the timeline and restores any version loss-free (safety commit + read-tree + restore commit). git-less systems degrade silently; linked/imported folders are never touched. Live-testing surfaced four real bugs fixed along the way — most notably uncapped requests turning aborted turns into slot-locking ghost generations (the endless 'Düşünüyor…'), enhance instructions poisoning engine history into brief-echo, and briefs mentioning 'mobil' hijacking the project profile to React Native.*

## Phase 4 — Productization (v1.0)

- [x] **4.1 Hybrid API mode** — optional OpenAI-compatible / Anthropic endpoint for weak hardware; local stays the default. *Shipped: Settings gains an OpenAI-compatible base URL + key + model with a mode selector (Off / Fixes Only / All Turns). Fix turns (iterating files + a repair signature) route through a stateless apiEngine (SSE streamed like local), with silent fallback to local on failure; the deterministic repair ladder still runs first so only genuinely hard fixes reach the paid model. This is the real fix for 'the small local model can't repair its own errors' — Bolt parity. Verified end to end against a stand-in endpoint: the turn hit the remote and streamed back into chat.*
- [ ] **4.2 Windows & macOS packaging** — *NOT COMPLETED — deferred. Needs per-platform Node binaries in `vendor/node-bin` (the llama worker runs outside Electron's V8 cage), electron-builder nsis/dmg targets, and real Windows/macOS hardware or CI to verify — none of which exist on the Linux dev machine. Parked until after Phase 5; the .deb flow remains the shipping path.*
- [x] **4.3 Multi-project workspaces + in-project search** — *Shipped: sidebar PROJELER section lists generated + linked projects (newest first, linked badge), one-click load via the safe scanner; code view search box matches paths and content case-insensitively with path:line previews. GUI-verified.*
- [x] **4.4 Remote model-catalog manifest** — update the Hardware Advisor's catalog without shipping a new app version. *Shipped: advisor.plan() fetches model-catalog.json from the repo (validated, 4s timeout), caches it, and merges over the embedded catalog (remote updates/adds, embedded keys always kept). Offline: cache → embedded → local buildPlan fallback. Verified: live GitHub manifest fetched in-app, override reflected in the plan, all embedded models preserved, cached to disk.*
- [x] **4.5 Local mini-benchmark** — a 2-minute on-device eval (generate a section → compile → score) so the Advisor shows real quality scores measured on *your* machine, not just speed grades. *Shipped: a "Mini-test" button on the loaded-model card gives every model the SAME fixed task (compact pricing section, one fenced block, grammar-free), measures real tok/s, checks the output compiles through the app's own layer-1 verifier, and scores 0-100 (compile 60 + speed 30 + substance 10). Results persist per model file (userData/benchmarks.json) and show as a "measured on this device" pill next to the Advisor's paper speed grades. Live-verified on the 3B: 6.1 tok/s, compiles ✓, 88/100 in 69s. Measurement lesson: the first cap (400 tokens) cut the closing fence and punished token budget instead of quality — compact task + 900-token ceiling.*
- [ ] **4.6 Local image generation** — stable-diffusion.cpp so generated sites get real, theme-matched images instead of placeholders. *NOT COMPLETED — deferred. A separate infrastructure effort (new binary service + multi-GB model downloads + generation queue) that competes for the same RAM/VRAM as the coder model on 16 GB machines. Parked until after Phase 5.*

## Phase 5 — The Debug Engine (v1.1.x) — THE NEW DIRECTION

**Mission:** build a first-class debugging engine — the capability that defines tools like Codex, Claude Code and Gemini CLI — that **finds faulty code by scanning**, **sees localhost errors the instant they happen**, **pinpoints the root cause**, and **fixes it both without a model (deterministically) and with one** — proven on simple projects first, then hardened level by level. All future development bends toward this.

**Why we can do this:** the pieces already exist in scattered form — the runtime error collector (3.2), the four-rung repair ladder with model-free rung 0 (3.3), the permanent error corpus (`npm run test:corpus`), pixel + vision self-review (3.3), git green-restore (3.4) and hybrid API routing for the hard fixes (4.1). Phase 5 fuses them into ONE engine with ONE brain, then grows the senses and the surgeon.

- [ ] **5.1 Debug Engine core — one pipeline, one brain.** A single `debugEngine` module that owns the whole loop: **Capture → Diagnose → Locate → Fix → Verify**. Every error — compile, runtime, visual, static-scan — enters the same pipeline, carries the same diagnosis shape (file, line, class, signature, excerpt), and exits through the same fix ladder. One visible event log ("Hata Günlüğü") so the user can watch the engine think: what was caught, where it was located, which rung fixed it, how it was verified. Today's scattered call sites (appStore runtime sub, build-check flow, visual review) become thin feeders into this one engine.
- [x] **5.2 Whole-project static scan — find the bug WITHOUT running.** A "Tara" (Scan) action that walks every project file in milliseconds: Babel parse per file (syntax), project import/export graph (unresolved or case-broken imports, imports of nonexistent exports), undefined identifiers and JSX components, React hooks used without import, leftover `{{MARKER}}`s, package.json sanity. Findings are ranked and fed straight into the 5.1 pipeline — the fixable classes repair themselves, the rest get precise line-numbered model turns. Runs on demand, before Run, and on freshly imported projects. *Shipped: `debugScan` (7 detector families) emits every finding as an autoRepair-compatible diagnosis, so scan findings ride the SAME repair ladder as runtime errors — one pipeline, one brain (`debugEngine`, the 5.1 seed). Three surfaces: a "Tara" button in the workspace header (applies model-free fixes + syncs to disk), a silent pre-Run scan (deterministic classes never reach localhost), and an automatic REPORT-ONLY scan on folder import (never writes to the user's original folder without a click). Suite: `npm run test:scan` — 9/9 (clean project = zero false alarms; 5 deterministic classes repaired AND verified; 3 model-tier classes correctly left). Live GUI proof: a project with a case-broken import + missing useState + undefined info.map was imported → auto-report listed all three untouched on disk → one "Tara" click repaired them into the original folder, no model loaded.*
- [x] **5.3 Fault localization — "the problem is HERE".** Map every stack trace / error signature to exact file + line + symbol (vite URL → project path is already done; extend to source-mapped lines, truncated stacks and minified frames). When the stack doesn't say, RANK suspects: import-graph distance from the crash site, most-recently-edited files (git history is already there), error-class priors from the repair telemetry. The chat answer changes from "something is broken" to "src/components/Menu.tsx:41 — `items` is used but never defined; 92% this file". *Shipped: `faultLocate` — direct-hit mode pulls file+line+SYMBOL from stack frames (92% confidence); truncated-stack mode ranks suspects by undeclared-identifier use (strongest signal), `.prop`-access placement for property-read crashes, and recency tie-break (updatedAt), refusing to invent suspects when there is no signal. Wired into the runtime flow: the 🌐 capture message now carries a "📍 KONUM: file:line (Symbol) — %NN: reasons" block for the user, and the hidden fix turn's line-numbered snippet is re-anchored to the primary suspect — vendor frames' huge line numbers used to scroll the window into the void on truncated stacks. Suite: `npm run test:locate` 6/6; live GUI proof: a prop-less `data.map` crash produced "📍 KONUM: Hero.tsx:18 (List) — %92" in chat. Honest limit: the line comes from vite's transformed module, so it can sit a few lines off the source (the ±12-line snippet window absorbs it) — true source-map decoding remains future work.*
- [x] **5.4 Live localhost watch — see EVERYTHING, instantly.** Today the hook catches `window.onerror` + unhandled rejections. Extend the injected hook to also stream `console.error`, failed network requests (4xx/5xx/refused), React error-boundary catches and vite's HMR error overlay events to the collector — deduped by signature, throttled, each one entering the 5.1 pipeline the second it fires. The dev page becomes a patient on a heart monitor. *Shipped: the hook now has four senses, each tagged with a `kind` so the pipeline picks the right policy — `error` (crashes → the existing auto-repair ladder), `console` (console.error carrying /error/i — React error-boundary reports ride this — same ladder), `network` (fetch ≥400/refused + broken `<img>`/`<script>` resources → an honest 📡 notice with "düzelt" hand-off, deliberately NO automatic model turn: a dead external API is not fixable by editing files), `hmr` (vite's error overlay read from its shadow DOM via MutationObserver → the compile-error protocol; the only signal that exists when a file is broken by hand, since window.onerror never fires). Signature dedup + a 20-report cap per page load; the hook's own collector POSTs are recursion-locked. Live-proven: a broken img produced the 📡 notice, and breaking Hero.tsx on disk with an "external editor" put the overlay's exact compile error into chat within seconds.*
- [x] **5.5 The dual-mode surgeon.** Deterministic first, model second, verification always: rung-0 classes keep growing through the corpus discipline (new class seen live → fixture first → deterministic branch); what rung 0 can't prove goes to surgical line-numbered model turns (hybrid API for the genuinely hard ones); every fix re-verified (layer-1 parse + build check + runtime re-listen) with git green-restore as the never-broken floor. Every decision appended to the repair telemetry so weak classes surface from field data. *Shipped: in "Fixes Only" mode the API is now truly the LAST resort — the first fix attempt for any error always runs locally (rung 0 already closed the free classes), and the turn escalates to the API only when the local model failed on the same signature (2nd runtime attempt / final auto-verify round). An optional ask-first switch in Settings ("API'ye göndermeden önce sor") replaces automatic escalation with a one-time 🤝 hint — typing `düzelt api` / `fix api` IS the consent and forces the escalated turn; with the API off nothing changes at all. Routing decisions land in the repair telemetry (api-turn / api-escalated / api-fallback-local). Matrix suite: `npm run test:routing` 8/8. Live E2E against a mock OpenAI endpoint: `düzelt api` routed the fix turn to the API, its surgical edit block landed in the original folder on disk, telemetry logged api-escalated — and the negative check confirmed a plain `düzelt` starts LOCAL generation with zero API requests.*
- [x] **5.6 Simple-project proving ground — the engine earns trust before it gets power.** Grow the error corpus from single-file fixtures into a suite of small intentionally-broken PROJECTS (missing import, dead data var, broken import path, syntax cut, runtime-only crash, blank render…). A harness runs the whole engine against each: measure find-rate, fix-rate, time-to-green. A capability is "shipped" only when the suite says so — same discipline that built Phases 1–4, now automated end to end. *Shipped: `npm run test:ground` runs 10 whole-project fixtures through the REAL pipeline via both entry channels — 'scan' (static discovery) and 'runtime' (verbatim collector diagnoses through locateFault + rung 0) — and prints the scorecard: find-rate 9/9, deterministic repair 7/7 (applied AND verified green), avg time-to-green 22ms (worst 96ms), zero false alarms on the clean project, model-tier classes correctly refused. `npm run test:engine` chains all five engine suites (corpus + scan + locate + routing + ground = 44 checks). The discipline paid out on the FIRST run: P8 (prop-less `.map` crash, truncated stack) exposed a real localization weakness — two files both using `.map` tied and the wrong one won; the engine now ranks a receiver that is an incoming prop/parameter (can be undefined) above one that is concretely declared or imported, and the fix landed with its own regression fixture in the locate suite.*
- [x] **5.7 Advanced debugging — from reactive to proactive.** Multi-error sessions (fix in dependency order, not arrival order), cross-file root causes (the crash is in A, the bug is in B), watch mode (continuous background scan while the user edits, findings appear as they type), and value probes — the engine injects temporary logging into suspect lines, reads the values from the collector, and reasons from DATA instead of guessing. This is where the tool stops imitating Codex/Claude/Gemini and starts being one. *Shipped, all four: (1) multi-error sessions — findings repair in DEPENDENCY order (syntax → imports → identifiers) with multi-pass rescanning, because a syntax break masks the same file's other bugs (P11: apostrophe cut + hidden missing import, both fixed in ONE call); (2) cross-file root cause — on a property crash whose receiver is a prop, the engine names the file that renders the component WITHOUT the prop as "KÖK NEDEN ADAYI", the file the stack never shows; (3) watch mode — an eye toggle in the workspace runs a debounced report-only scan as files change (never edits under the cursor), findings as a badge with file:line tooltips — live it even caught a junk file a model turn had created; (4) value probes — the suspect expression is temporarily wrapped (`data.map` → `__nxProbe('data', data).map`), the page is force-rendered offscreen (React unmounts a crashed tree — HMR alone never re-runs the module), the value at the moment of the crash streams back (🔬 "data = undefined"), the file is restored under guarantee, and the fix turn's prompt starts from MEASUREMENT, not guess. Two hard-won race lessons: the probe waiter must be armed BEFORE the sync (vite's build window ate the data), and probe delivery must bypass ALL busy guards — the same crash emits error+console twice, the second starts a turn, and a busy guard was dropping the measurement (probe-timeout ×4 before the live proof: probe-hit + restored file + 🔬 in chat). Suites: `npm run test:engine` = 6 suites, 51 checks, all green.*

## Phase 6 — The Engine, 10× (v1.2.x) — FROM DEBUGGER TO COLLEAGUE

**Mission:** take the Phase 5 engine from "it catches and fixes" to **Codex / Claude Code / Antigravity class**: compiler-grade eyes, a *real* runtime debugger instead of string probes, edits that cannot miss, verification that *uses* the product like a human tester, and an engine that **learns from its own field data**. Phase 5 built the reflexes; Phase 6 builds the judgment.

**Why these seven (the honest audit):** field telemetry says rung 0 misses **57%** of live errors (39/69 `kat0-miss`); value probes hit only 3/11 in the field and mutate user files to measure; the 14B fabricated SEARCH blocks three times in one night (data-array copy — the matcher has no line/AST anchor to save it); an aborted turn left a 254-div spiral in the store (no turn atomicity); the scanner is regex-grade, so every false-positive class costs a manual fix; "verified" still means *compiles + not blank*, not *works*; and the engine writes telemetry it never reads. Each item below kills one of these, with the industry's current best practice as the bar (Claude Code shipped native LSP diagnostics in late 2025; Antigravity verifies by driving a real browser and hands the user artifacts; Google's agentic-APR work shows reproduction-first repair beats guess-first repair).

- [x] **6.1 A real runtime debugger — CDP `Debugger` domain, not string probes.** Attach Electron's `webContents.debugger` to the offscreen page, `setPauseOnExceptions`, and when the crash fires read the ACTUAL call frames and scope variables at the moment of death — exact file/line from the source map, every local's value, the full async stack — with **zero file mutation** (retires the inject-sync-restore probe dance and its race conditions; keeps `__nxProbe` only as a fallback for non-exception measurements). The fix turn's prompt starts with "at List (Hero.tsx:4), `data` = undefined, caller `<List />` passed no props" — measured, sourced, undeniable. Also closes the 5.3 source-map honesty gap for free. *Shipped: `debugInspect` opens the dev page offscreen with the debugger attached and hands back user-code frames with real locals and the source-mapped ORIGINAL line — live proof: "🔎 Debugger (çökme anı): Hero.tsx:5 — List() · data = undefined", the exact source line of the `.map`, file md5 untouched, probe fallback never needed. The source-map line decoder is dependency-free VLQ (self-validated against esbuild's real maps, `npm run test:sourcemap` 6/6, in test:engine = 7 suites / 57 checks). Three live-diagnosed lessons are IN the code: an absolute outer `Promise.race` because an inner await hung before the inner timer could guard it (and froze the runtime handler's `probing` latch with it); debugger commands never resolve on a webContents that has not committed a navigation — commit `about:blank` first, then enable, then navigate; and pause on `'all'` + skip-vendor-frames-until-user-code, because React catches render errors internally and rethrows from `commitRootImpl` — the 'uncaught' pause fires where the user frame is already gone.*
- [x] **6.2 Compiler-grade diagnostics — TypeScript language service over the in-memory project.** Embed TS's `createLanguageService` on the artifacts store's virtual file map (no node_modules, no install, fully offline): real type errors, truly undefined symbols, unused/missing imports, wrong prop types — the diagnostics class Claude Code gets from LSP, computed on OUR file map in milliseconds. The regex scanner stays as the fast first pass and the fallback for non-TS files; every scanner false-positive class (the `function List({ data })` lesson) becomes structurally impossible. Findings keep flowing into the same 5.1 pipeline diagnosis shape. *Shipped: `tsDiagnostics` runs a cached, incremental language service over the in-memory map — TS's own lib.d.ts set (100 files) and @types/react(-dom) are baked into the bundle as lazy raw chunks at BUILD time (`import.meta.glob` — which silently matches nothing if you forget the renderer's vite root is `src/`, hence the missing-default-lib self-disable guard), an ambient `declare module '*'` keeps unknown bare imports quiet, and a curated code whitelist (2304/2305/2339/2551/2552/2554/2741) keeps the signal clean. TS2304 findings are translated into the repair ladder's diagnosis shape and get FIXED deterministically; the rest go to the model with compiler wording. Live GUI proof: an imported project showed both layers side by side — `useState` auto-repaired by rung 0, and `TS2551: Property 'fiyatt' does not exist… Did you mean 'fiyat'?` reported at the exact line, a class no regex could ever see; after the import insertion the finding's line tracked 10→11 (incremental service). Suite: `npm run test:tsdiag` 6/6 (zero noise on a clean project is an explicit assertion); test:engine = 8 suites / 63 checks.*
- [x] **6.3 Edits that cannot miss — anchor ladder for the applier.** Three new tiers under SEARCH/REPLACE: (1) **line-anchored edits** — the numbered snippet we already send becomes a contract, the model cites the line range and we verify content before applying; (2) **AST-anchored replace** — "the `desc` property of the object whose `title` is 'Atlas E-ticaret'" resolved via Babel paths, immune to whitespace and copy fabrication; (3) **reality feedback** — when a SEARCH misses, the retry automatically embeds the REAL bytes of the nearest match so the model corrects itself instead of hallucinating twice (the exact failure that cost three rounds on the 14B night). Target on the proving ground: unmatched-block rate ≈ 0. *Shipped as three applier tiers plus the feedback loop: line-number-prefix stripping (models copy "  12| code" from the numbered snippet, prefixes now peel off before matching), a single-line `key: 'value'` similarity anchor (bigram Dice ≥0.55 with a uniqueness margin — the exact class the 14B missed three times now lands on the right `desc:` line even from a slightly-wrong copy, and REFUSES when two candidates tie: no wrong repairs), and **reality feedback**: `applySearchReplace` now returns the failed SEARCH texts, and one automatic hidden retry (per user turn) shows the model the file's REAL bytes around the nearest match — "eşleşmedi" alone teaches nothing, the actual lines do. Live-proven with a fabrication mock replaying the 14B failure: request 1 fabricated → 0 applied → 🎯 retry carried "GERÇEK içeriği" (mock log `hasReality: true`) → verbatim copy applied, exactly one retry fired. Suite: `npm run test:anchor` 6/6 (old tiers regression-guarded); test:engine = 9 suites / 69 checks.*
- [x] **6.4 Turn transactions — nothing half-applied, ever.** Every generation/iteration applies into a shadow overlay; commit to store+disk only on clean completion, and an abort/crash/violation rolls back atomically (the 254-div spiral becomes impossible). The overlay also gives free "what did this turn touch" diffs for the Debug Panel and the git history subjects. *Shipped as snapshot + atomic revert (the design keeps the Bolt-style live-apply UX untouched — files still stream into the editor): every UPDATE turn snapshots the pre-turn contents of existing files; a user abort atomically restores every modified existing file, posts "↩️ … geri alındı (N dosya)", and logs `turn-rollback` telemetry. Files newly CREATED during the turn are deliberately kept — the planned-build "Durdur at 11/12 and continue" workflow the owner actually uses stays alive, and plan/new-file turns take no snapshot at all. Live-proven by restaging the 254-div class with a slow-streaming mock: garbage edit was live-applied into the store mid-stream (verified present), Durdur pressed, App.tsx came back byte-identical and the junk was gone.*
- [x] **6.5 Behavioral verification — the engine USES the site (Antigravity parity).** After Run, an offscreen CDP session walks the page like a tester: every nav link clicks and scrolls to a real section, buttons respond, forms validate, images actually loaded (natural size > 0), console stays clean while interacting — and produces **artifacts**: a per-section screenshot strip and a pass/fail interaction report in chat. "Doğrulandı" stops meaning "rendered" and starts meaning "works". Defects flow into the same pipeline with the interaction trace as diagnosis. *Shipped: 12 seconds after Run, `behaviorTest` walks the page offscreen — clicks every `#`-anchor nav link (target section must EXIST, page must scroll), presses up to 8 buttons (a click that throws is a defect), fills and submits the form (navigation blocked via will-navigate), measures every `<img>`'s naturalWidth, watches the console throughout, and scrolls section-by-section capturing a screenshot strip to `cache/behavior/`. The report lands in chat ("görseller 1/2 ✗ · menü bağlantıları 2/3 ✗ · butonlar 2/2 ✓ · form ✓ · konsol ✓ · 📸 5 kare") and failures arm the one-word "düzelt" hand-off with the interaction trace. Errors the walk provokes flow through the existing hook senses — one pipeline, no duplication. Live proof with two planted defects: the broken image was named by URL, the dead `#olmayan-bolum` nav link was flagged, buttons/form/console passed, and the artifact strip shows the engine's own typing in the form fields ("test@nexora.dev", "Merhaba, deneme mesajı.").*
- [x] **6.6 Reproduction-first repair — write the failing test, then fix until it passes.** For every runtime defect the engine can parametrize (crash on render, handler throws, missing section), it first generates a minimal repro check (in-page assertion or vitest when installed), CONFIRMS the repro fails, then loops the fix ladder until the repro passes AND the whole verify suite stays green — Google-APR style, ending "fixed" claims that fix nothing. Repro artifacts persist next to the repair telemetry. *Shipped as the repro seal: the repro "test" is a fresh offscreen load watched for the error's normalized signature (prefix/location/URL-stripped core, so vite's shifting line numbers don't break matching — `npm run test:reprosig` 5/5 including the "short signature matches everything" trap). BEFORE a model turn: the pre-repro gate — a signal that doesn't reproduce on a fresh load (stale HMR report, mid-edit snapshot) costs no turn and refunds the attempt. AFTER any repair (rung 0 or model): the post-repro seal — "✅ hata artık üretilmiyor — onarım kanıtla doğrulandı" or the honest "⚠️ HÂLÂ üretiliyor", with repro-verified/failed/transient telemetry feeding 6.7. The honest path earned its keep immediately in live testing: a ⚠️ repro-failed exposed that a "repaired" file had never reached the disk — the pre-Run scan fixed the store but handleDev synced a STALE React closure of the files; without the seal that lie would have stood (fixed: fresh read from the store). The 3B's failed attempts on the prop-crash case each ended with the honest ⚠️ instead of silence. All three verdicts are live-proven: ⚠️ twice on the 3B's failed repairs, ℹ️ on a fake signal against a crashing-but-different page (zero turns burned), and the golden chain — crash report → rung-0 fix → 8 seconds later `repro-verified` → "✅ … onarım kanıtla doğrulandı" in chat.*
- [x] **6.7 The learning engine — telemetry becomes judgment.** The repair log stops being a diary and becomes priors: per-error-class success rates route the ladder (a class that rung 0 never fixes skips straight to the surgeon; a class the local model always botches escalates immediately when the API is consented), per-project fix memory feeds the prompt ("this project's `data.ts` exports were renamed once already"), and a `motor karnesi` view in Settings shows find/fix/green rates from REAL usage. The kat0-miss stream is mined for new deterministic classes — each becomes a corpus fixture first, per the standing rule. Target: kat0-miss from 57% to under 25%. *Shipped: a pure core (`errorClass.ts` — classifier + aggregator + priors, `npm run test:learn` 9/9) turns the log into class stats, and two conservative priors now route the runtime ladder: `skipKat0` (a class rung 0 has missed ≥5 times with zero hits goes straight to the surgeon) and `escalateEagerly` (a class with ≥3 repro-failures and zero verifications asks for API escalation on the FIRST attempt — consent rules unchanged). Learning is two-way: a single counter-example switches a prior back off. Every applied prior is telemetry itself (`priors-applied`). The Settings scorecard runs on REAL field data — live it read 118 events and told the truth: rung-0 hit rate 21%, and `property-read: kat0 0✓/18✗` — a class the priors would already reroute today. The kat0-miss mining surface is the scorecard's own class table. (Per-project fix memory deferred to the Debug Panel work in 6.8, where turn diffs land.)*
- [x] **6.8 The visible engine — a Debug Panel worthy of the name.** One dedicated surface (the 5.1 promise, delivered): a live timeline of Capture → Diagnose → Locate → Measure → Fix → Verify with each step as a card — the error, the 📍 suspect list, the debugger's variable snapshot, the diff the fix applied, the behavioral-verification screenshot strip. Users stop reading tea leaves in chat and watch the engine think; every card links to its telemetry entry. Trust is a feature. *Shipped as the "Motor" tab in the workspace: a live event stream fed by the SAME single choke-point every engine decision already passes through (`logRepair` became dual-write: file telemetry + in-session timeline — zero new instrumentation at any call site, so every future engine feature appears here automatically). Each card: step name (Yakala/Tanıla/Ölç/Onar/Doğrula/Koru/Tırman/Öğren), the raw layer chip, a timestamp and the decision detail; capped at 200 events; lifetime statistics live in the Settings scorecard (6.7). Live-verified: import → Tara produced "🔧 Onar · scan-kat0 · eksik React import'u eklendi: useState; tanımsız 'bilinmeyenVeri' … stub'ıyla onarıldı" in the panel seconds after the click (screenshot 29-debug-panel.png).* **PHASE 6 COMPLETE.**

## Phase 7 — The Agent Layer (v1.3.x) — FROM ENGINE TO COLLEAGUE

**Mission:** Phases 5–6 built a debug engine that already *verifies like* Antigravity (behavioral walk, screenshots, repro seal) and *repairs like* Codex (deterministic ladder, reality feedback). What separates the app from the Codex / Antigravity **experience** is no longer the engine — it is the **agent workflow around it**: work is still one synchronous chat turn, evidence is scattered across chat bubbles instead of reviewable documents, diffs are single-file, trust is a modal instead of a policy, the terminal is invisible, and everything the engine learns dies with the session. Phase 7 closes exactly that gap — the *delegate → observe → review evidence → steer* loop that makes those tools feel like colleagues.

**The honest audit (why these eight):** Codex's most-copied design is the two-layer security model (sandbox = what is *possible*, approval policy = when to *ask*) plus a live `update_plan` checklist and a git-truth review pane with inline comments that feed back to the agent; Antigravity's is Artifacts ("verify with artifacts, not logs": implementation plan → live task list → walkthrough with screenshots), Google-Docs-style comments that steer without stopping, and a knowledge base that makes learning a core primitive. We already produce the raw material for almost all of this (plan turns, behavior screenshots, Motor timeline, repair telemetry, git history) — it is packaged as chat bubbles instead of product surfaces. And the anti-lessons are just as documented: Antigravity's launch shipped prompt-injection exfiltration paths, a `webhook.site` default allowlist, and a Turbo mode that deleted a user's D: drive via an unvalidated path — every 7.x item below treats *local-first + validated + default-deny* as non-negotiable.

- [x] **7.1 The live task list — the agent shows its plan while it works.** Upgrade plan-first mode from a one-shot approval into a living artifact: every multi-step turn (planned build, multi-error debug session, behavior-fix loop) maintains a visible checklist — pending / in-progress / done, updated as each step completes — rendered as a pinned card in chat (Codex `update_plan` parity). The planned build's per-file orchestration and the debug engine's multi-pass sessions already have the step structure internally; this surfaces it. A step that fails shows *why* inline. Mid-conversation additions refresh the list instead of restarting it (Antigravity task-list parity). *Shipped: the task card is a `ChatMessage` with a `tasks` payload — it rides the session save for free, lands at the right transcript position, and `sticky top-0` pins it to the top of the chat while active (measured live: content scrolled 916px past the fold, card top stayed 24px under the header — the user watches the current step while the log streams below). A pure core (`src/lib/taskList.ts`, `npm run test:tasklist` 11/11 — immutability, silent no-op on unknown targets, running→failed on finish, stale-card deactivation on session load) + three thin store wrappers. Two feeders wired: the planned build replaces its static "plan onaylandı" list with a live card (auto files pre-ticked "otomatik — kod yazdı", per-file running→done with an honest "2. denemede" note when the retry saved it, failed + reason when it didn't), and the Tara repair session renders every finding as a ✓/✗ checklist row with the repair note. Live-proven in the real renderer (browser mode, scripted-model mock): golden run 8/8 with a staged first-attempt failure recovering "2. denemede"; Durdur mid-build closed the card "⏹ durduruldu" — finished steps ✓, never-started steps honestly pending; scan card named all three planted defects with their fixes. Mid-conversation `appendTaskSteps` is core-ready and tested; the behavior-fix loop feeder joins when 7.2 packages its artifacts. The hard-won lesson was in the TEST HARNESS, not the feature: the browser-mode `window.nexora` mock had rotted since Phase 5 — missing namespaces dropped the whole React tree at mount (Sidebar's `projects.list`), wrong return shapes crashed it again (`sessions.list` returns a bare array, not `{ok, sessions}`), and a mock `chat.send` that resolves before its stream finishes makes every orchestrated flow "complete" instantly with zero files — the mock now mirrors real IPC semantics, which makes browser mode a genuine test surface for every future 7.x item.*
- [x] **7.2 Artifacts — evidence as documents, not chat scroll.** Three first-class, on-disk, reviewable documents per task, stored under the session (`Sessions/<id>/artifacts/`): the **Implementation Plan** (pre-execution, already exists as the plan turn — now saved and versioned), the **Task List** (7.1, persisted with `.resolved.N` history), and the **Walkthrough** (post-completion markdown: what changed and why, how to test it, "Features Implemented", and **Verification Evidence** — the behavior walk's screenshot strip, the repro seal verdict, the build/test results embedded inline). The engine already produces every input; the walkthrough composer just assembles them. Artifacts open in their own viewer tab; "Doğrulandı" becomes a document you can *read*, not a claim. *Shipped: `artifactDocsService` writes `~/NexoraAI/Sessions/<id>.artifacts/` (visible, syncable, same convention as everything else) with Antigravity-style `.resolved.N` versioning — same bytes never bump a version, a name whitelist keeps IPC input from ever becoming a path, and old versions stay readable (`npm run test:artifacts` 14/14 covers composer + real-disk versioning + the traversal guard). Three writers, one choke point each: plan approval saves `implementation_plan.md`; EVERY closing task card dumps `task.md` from inside `taskCardFinish` (the 6.8 logRepair lesson — future feeders get the document for free); and the walkthrough composer (`src/lib/walkthrough.ts`, pure) assembles request → file checklist → features (internal `[şablon:]` tags stripped from the user-facing doc) → how-to-test → verification evidence. The walkthrough is a LIVING document: `postGenVerify` writes v1 with the build verdict, the behavior walk rewrites it with rows/defects/screenshot strip embedded — the previous version slides into `.resolved.N`. A "Belgeler" tab renders it with a dependency-free markdown-lite (no `dangerouslySetInnerHTML`), doc cards showing time + older-version count. Live-proven end to end in the real renderer: plan → build → "📄 Walkthrough hazır" notice → three docs listed → behavior evidence rewrote the walkthrough (v0 honestly said "davranış testi henüz koşmadı", v1 embeds "görseller 1/2 ✗", the dead `#olmayan` nav target and the screenshot strip) — and the old version still reads back byte-perfect. Honest edges: repro-seal verdicts still live in the Motor tab only (they join the walkthrough when a runtime-repair feeder lands), and single-shot (non-planned) builds don't compose a walkthrough yet.*
- [x] **7.3 The review pane — git-truth diffs, multi-file, hunk-level.** Replace the single-file DiffModal flow with a real review surface: all changes of a turn (or since last accept / on the working tree) as collapsible per-file diffs computed from git state — including the user's own manual edits — with stage/revert per file *and per hunk*. The 6.4 turn snapshots and 3.4 git history already know what each turn touched; this is their UI. Scope selector: this turn / uncommitted / since green tag (Codex review-pane parity). *Shipped: the modal became a review surface with three scope chips — **Bu tur** (6.4 turn snapshot), **Son kayıttan beri** (git HEAD) and **Yeşil sürümden beri** (nexora-green tag) — all flowing through one diff core (`computeDiffs`: any base vs the live store, so the user's manual editor edits are always included). Git truth comes from a new deliberately-electron-free reader (`gitRead.ts` — `filesAtRef` runs `ls-tree` + `show`, READ-ONLY, ref whitelisted so IPC text can never become a git argument; linked folders read the user's own HEAD without ever writing). Hunk math is pure and zero-tolerance-tested: `extractHunks` + `contentWithHunkReverted` (revert one hunk, the others LIVE; revert all = base byte-perfect; `npm run test:review` 15/15 including a real temp git repo and a `HEAD; rm -rf /` injection refusal). Per-file revert handles all three statuses (modified → base, added → file deleted, deleted → file restored); reverts write to the workspace and enter the timeline with the next accepted generation (honest footer says exactly that). The old accept/reject footer survives on the turn scope — nothing regressed (live-verified). Live proof: a staged two-hunk edit showed "DEĞİŞİKLİK 1/2 / 2/2" headers, reverting hunk 2 restored the CTA while the title hunk lived, "Dosyayı geri al" deleted a freshly-added file and restored a HEAD-deleted one, and the git scope rendered modified+added+deleted against a HEAD base. Deferred honestly: inline comments on diff lines are 7.4's whole point, and 'stage' as a separate index doesn't exist here — accept IS the stage.*
- [x] **7.4 Comment-to-steer — feedback that doesn't restart the task.** Inline comments on review-pane diff lines and on artifact documents (plan lines, walkthrough sections, even screenshots) become targeted instructions for the next agent turn — batched and attached with exact file:line / artifact anchors, so "bu buton yanlış renkte" lands as a surgical edit request instead of a new vague brief. Comments queue while a turn is running and feed the next turn without killing the current one (Antigravity comment parity; Codex inline-diff-comment parity). *Shipped: hover 💬 on any review-pane diff row (rows now carry their after-side line number) and on any walkthrough/plan/task-doc section heading opens an inline draft — Enter queues it. The queue lives in the store and is consumed by the first ELIGIBLE turn only: hidden fix turns, planned per-file turns and plan/brief meta-turns never eat comments, and a running turn is never touched (live-proven: a comment added mid-turn left the running prompt clean, waited in the chip — which honestly says "sonraki tura iliştirilecek" — and landed in the next turn's prompt). The pure composer (`steerComments.ts`, `npm run test:steer` 10/10) turns each comment into a surgical instruction: `path:line` header, ±2 numbered context lines from the file's CURRENT bytes with a `← COMMENT TARGET` marker (so the SEARCH block copies real bytes — the 6.3 lesson applied to humans), honest fallback to the reviewed excerpt when the line no longer exists, `[doc § section]` anchors for document comments, 12-comment/300-char caps with the overflow declared. A chat chip shows the queue (`💬 2 yorum sırada · Hero.tsx:7 · § Özellikler`) with "Şimdi uygula" / discard; attaching posts a transparent "💬 N inceleme yorumu bu tura iliştirildi" note before the streaming bubble. Both original honest edges were closed on request: comments now anchor to the screenshot ITSELF (hover 💬 on the image → `[walkthrough.md § 🖼 sec-1.png]` — the honest version of pixel anchors, since section-named frames tell the model exactly which page section is meant), and the queue persists in the session file (`SessionData.comments`) — restart or session-switch restores each session's own queue (live-proven: queued in session A, cleared on new session, restored with the chip on reopening A).*
- [x] **7.5 Two-layer trust — sandbox separate from approval policy.** Split today's per-action PermissionModal into Codex's two questions. Layer 1, *capability*: `[RUN]` executes with workspace-scoped writes (project dir + a temp dir; `..`/absolute paths refused and root-path patterns like the `rmdir /q d:\` class structurally rejected), a default-deny allowlist for anything beyond, and the existing denylist hardened into path validation. Layer 2, *consent presets*: **Salt Okunur** (agent edits nothing, proposes only), **Otomatik** (default — free rein inside the workspace, ask at every boundary), **Tam Erişim** (explicit, per-project, loud). Per-command allow/deny lists editable in Settings; every escalation logged to the Motor timeline. No Turbo-mode-style silent full-auto — Antigravity's drive-deletion case is the standing counterexample. *Shipped: one pure shared core (`electron/shared/trust.ts`) both processes import — the renderer decides, the MAIN process re-checks (defense in depth: a compromised renderer still cannot run a 'deny'). Verdicts: 'deny' (sudo/shutdown/fork-bomb, pipe-to-shell `curl|sh`, destructive commands aimed at root-class paths — `rm -rf /`, `~`, `$HOME`, `d:\` — or escaping via `..`; command CHAINS are split and judged per segment, so `npm build && sudo cp` still denies), 'auto' (npm/npx/vite/tsc/git-read-only class inside the workspace), 'ask' (absolute/`~` paths, network tools, unknown commands — default-ask, with Windows `/s`-style flags correctly NOT read as paths). User allow/deny prefix lists in Settings; deny beats allow, and NOTHING beats a hard deny — not even the user's allowlist. Fetches have NO default domain allowlist (the webhook.site lesson). Consent presets behave exactly as specified and are live-proven per tier: Otomatik ran `npm install` unasked, blocked `rm -rf /` with the reason in chat, and asked for `git push` with "neden soruluyor" in the modal; Salt Okunur ran nothing, listed the 2 proposed actions honestly, and now also flips auto-apply off so file edits go through accept/reject; Tam Erişim ran `git push` + fetch unasked while `sudo` STILL died; a user deny-list entry killed `git push` even in Full. trust-deny/trust-ask events land in the Motor timeline (Koru step). `npm run test:trust` 31/31 — including the Antigravity `rmdir /s /q d:\` case as a permanent fixture.*
- [x] **7.6 The visible terminal.** A real terminal panel in the workspace: every `[RUN]`/`[PKG]`/`[DEV]` execution streams its live stdout/stderr there (today's ~20KB post-hoc capture becomes a live feed), each command a card with exit code and duration, and the user can run their own commands in the same panel (through the same layer-1 sandbox). The agent's world stops being invisible; Antigravity's editor+terminal+browser triad completes (editor ✅, browser ✅ via the offscreen CDP walk, terminal — this). *Shipped: a pure streaming runner (`procRun.ts` — every stdout/stderr chunk fires `onChunk` the moment it arrives; the return keeps the old `RunResult` contract so nothing upstream broke) feeds `TERM_OUTPUT` IPC events into a dedicated `termStore` (no import cycles — appStore, agentActions and the panel all feed it). The Terminal tab renders each execution as a card: source chip (**ajan** / **sen** / **dev**), live-streaming output, spinner → exit code + duration badge; agent `[RUN]`s register a card automatically (chat still gets the summary — two surfaces, one execution), `[DEV]` streams its status lines, and the prerequisite `npm install` a command triggers streams into the SAME card. The user prompt at the bottom runs through the SAME two-layer trust: typing is consent for the 'ask' tier, but 'deny' is still a wall (live-proven: `rm -rf /` typed by the user died with "🛡 engellendi · yıkıcı komut kök yolu hedefliyor") and Salt Okunur blocks the terminal too. `npm run test:procrun` 8/8 — and the suite's first run caught a REAL pre-existing bug: killing the `shell:true` wrapper never killed its children (`sleep 5` survived a 300ms timeout for the full 5s because open grandchild stdio held the 'close' event), fixed with POSIX process-group kill + a settle guard so timed-out commands now resolve in ~500ms with their orphans dead. Live-proven in the renderer: mid-run screenshot shows the spinner with the first chunk already visible, the finished card shows both streamed lines + "kod 0 · 0.6s". Honest edges: it's an execution panel, not a PTY (no interactive stdin/ncurses), and dev-server cards carry status lines rather than raw vite stdout. LESSON (browser-mode harness): module import chains run BEFORE the main.tsx mock body — event subscriptions in store modules need a deferred retry or they silently bind to nothing.*
- [x] **7.7 The task queue + inbox — delegate-then-review, honestly local.** Queue multiple named tasks; the single local model works them one at a time (16 GB reality — no fake parallelism), each task in a 6.4-style transaction with its own artifacts, and finished work lands in an **inbox**: task cards with state (queued / running / needs-review / verified), the walkthrough, and the review-pane diff one click away. Follow-ups typed mid-turn queue for the next turn instead of interrupting (Codex Tab-to-queue parity). Optional per-task git branch so parallel *review* (not parallel execution) never collides — the local, truthful version of Codex worktree threads and Antigravity's Agent Manager. Also picks up the 7.2 deferrals by construction: once every job (single-shot builds, runtime repair sessions, behavior-fix loops) carries a task identity, it gets a 7.1 card + 7.2 walkthrough automatically — and repro-seal verdicts land in their task's walkthrough instead of living only in the Motor tab. *Shipped: a pure state machine (`taskQueue.ts` — FIFO, one runner at a time, illegal transitions are silent no-ops so races can never corrupt the queue; `npm run test:queue` 11/11) driven by a single-instance processor in the store: send the task's prompt → an emerging plan is auto-approved (delegation IS the consent — Agent Decides) → wait out streaming, auto-repairs AND the post-gen verify chain (which starts LATE, after the lazy prettier chunk — the processor waits for it to rise before judging, a race the live test caught as a wrong "yanıt hazır" verdict) → verdict: **verified** (verification clean) / **needs-review** (an error survived) / **failed**. Typing mid-turn queues instead of interrupting (live-proven: two tasks typed during a running turn left it uninterrupted, then ran back-to-back to "üretildi · doğrulama temiz"); the unrepairable-build path landed honestly at "⚠ incele · doğrulama hata bıraktı". The inbox lives in the chat header (amber when something needs review) with per-task state chips, duration, one-click ⇄ Review (7.3 pane) and 📄 Walkthrough (7.2 doc); queue persists in the session (a run interrupted by a restart honestly reopens as needs-review). The 7.2 deferrals are closed: single-shot builds now compose a walkthrough from their touched files, and repro-seal verdicts flow into the open walkthrough through the same logRepair choke point (task-verified/task-review events land in the Motor timeline too). The "per-task branch" promise shipped in its HONEST form: with sequential execution a branch is theater — the real need is "what did THIS task change?", so every task seals the timeline tip (`baseHash`) when it starts, and the inbox's ⇄ İncele opens the review pane on a **Görev tabanı (hash)** scope diffing the workspace against that task's base (live-proven: after a task ran, review opened against `abc1234` showing exactly the task's change). Three tasks back-to-back = three independently reviewable diffs, no branches to babysit.*
- [x] **7.8 The project knowledge base — learning as a core primitive.** The 6.7 deferral, delivered at Antigravity Knowledge-Item scope: at session end a cheap local pass distills *project facts worth keeping* — architecture decisions, renamed exports, "this project's forms use X", recurring fix patterns, user corrections — into per-project markdown Knowledge Items (`~/NexoraAI/Projects/<slug>/knowledge/`), each editable/deletable in Settings, auto-loaded into future sessions' context (budget-aware, summaries first). KURALLAR.md grows hierarchical merge (global `~/NexoraAI/KURALLAR.md` → project) per the AGENTS.md standard's discovery rule. One counter-example retires a stale item — same two-way discipline as the 6.7 priors. *Shipped with one deliberate upgrade over the spec: items are distilled DETERMINISTICALLY at the moment of evidence, not by a model pass at session end — a small local model would hallucinate "facts", the engine's own signals cannot. Three feeders through existing choke points (zero new instrumentation, the 6.8 lesson twice over): kat0/scan repair notes → `repair-pattern` items, repro-verified fixes → `verified-fix` items carrying their signature, and 7.4 review comments → `user-preference` items ("bu buton amber olmalı" is said once, ever). Two-way discipline is literal: a repro-failed with a matching signature RETIRES the verified-fix item (live-tested: one counter-example, item gone, unsigned items untouched). Items are visible markdown in `Projects/<slug>/knowledge/` — same-title re-learning bumps a hits counter instead of duplicating (×2 badge), the 30-item cap evicts the least-trusted-oldest, and name whitelists block traversal. Every turn opens with a budget-aware `PROJECT KNOWLEDGE` block (most-trusted first; a too-long first title is clipped, never silently dropped — a test finding) and the KURALLAR block went hierarchical: global `~/NexoraAI/KURALLAR.md` merges before project rules with the project winning conflicts (both editable in Settings, next to the new Knowledge Base panel with per-item delete). Live chain proven end-to-end: Tara's useState repair became a repair-pattern item, an applied review comment became a user-preference item, and the very next turn's prompt carried both in its knowledge block plus the merged global→project rules. `npm run test:knowledge` 10/10 (real disk: dedupe/hits, cap eviction, sig retirement, budget clipping, merge order).* **PHASE 7 COMPLETE.**

## Phase 8 — Real-Time Hardening (v1.4.x) — THE CLOCK IS THE ENEMY

**Mission:** Phase 7 shipped the agent experience and every browser-harness test was green — then the very first end-to-end run on the REAL app with a REAL model (the Zeytin Kafe live test, 2026-07-07) exposed a whole class the harness cannot see by design: **real-duration races and a busy inference server**. Turns that take minutes, verify chains that overlap timers, an abort that doesn't reach llama-server. The packaging was never the suspect — the identical code stalls under a real clock and flows under a mock one (the same queue that froze in production ran itself in the browser). Phase 8 makes the agent layer honest under real time, and makes the test surface able to SEE time.

**The live-test ledger (why these seven):** the queue processor never self-started (twice) because its only trigger — the `sending` falling edge +1.2s — raced the minutes-long verify/repair chains; the behavior walk's one-shot 12s timer landed on a running auto-repair turn and silently never ran; a watchdog-cut penalty turn hung at 0 bytes for 36 minutes while llama-server chewed a request abort never cancelled — and Durdur made it WORSE (the reality-feedback retry opened a fresh hidden turn), leaving restart as the only exit; a task that never added the requested line was stamped "verified" (the verdict measures compile-clean, not goal-met); and everything the engine learned went into the default `nexora-projesi` folder because planned builds don't write a package.json.

- [x] **8.1 The hang chain — no turn may ever freeze the app.** A stream-liveness sentinel (N seconds with zero bytes on an active turn = the turn is declared dead, honestly reported, engine unlocked); Durdur becomes ABSOLUTE — it also cancels the hidden retry/penalty/queued-hidden turns instead of letting one reopen the wound; and abort must actually cancel the llama-server task (server-side cancellation / connection teardown that stops decode, verified against a deliberately busy server). The 36-minute zombie becomes structurally impossible.
- [x] **8.2 The queue heartbeat — delegated work may never sleep.** Replace the single-shot falling-edge trigger with a persistent heartbeat: while anything is queued, the processor re-knocks until the engine is genuinely idle, and the inbox card says WHY it waits ("motor meşgul: onarım turu koşuyor") instead of a frozen "sırada". Verified against real multi-minute verify chains, not mock timing.
- [x] **8.3 Behavior walk that retries.** The one-shot 12s timer becomes schedule-until-done: if the engine is busy when it fires, it re-arms (bounded, logged) — after Run, the walk ALWAYS eventually runs or honestly reports why it could not.
- [x] **8.4 Goal-met verdicts — "verified" must mean the request happened.** A task's verdict gains a goal check: deterministic content assertions derived from the prompt where possible (requested text/values present in touched files), behavior-walk evidence where visible — a task that compiled but didn't do the job lands as "⚠ incele: istek karşılanmadı", never "verified".
- [x] **8.5 Project identity — no more default-folder pileup.** Planned builds write a real package.json (name derived from the brief), the identity fallback warns loudly, and knowledge/rules/history stop silently commingling under `nexora-projesi`.
- [x] **8.6 Small honest fixes from the ledger.** The plan derives a Gallery section when the brief asks for one; task turns re-arm the walkthrough context so the document keeps versioning after queue work; inbox timestamps/status text stay live.
- [x] **8.7 A test surface that can see time.** The browser harness gains a timing-realistic mode (multi-second turns, slow verify chains, a busy-server simulator) so race-class bugs die in preview; plus a real-engine smoke suite (`test:e2e` with a small real model or a latency-faithful llama-server stub) covering exactly the killers: queue-under-load, abort-against-busy-server, behavior-timer-vs-repair-turn.

## Phase 9 — Universal Prompt Fidelity (v1.5.x) — ANY PROMPT, EXECUTED FAITHFULLY

> **✅ SHIPPED + LIVE-TESTED (2026-07-08).** The exact Gemini portfolio spec (Tailwind **v4**, named Navbar/Hero/Projeler/Footer, exact Turkish copy, exact class strings, Unsplash URLs, `#000000`/`#F59E0B`) was pasted against the **local 3B** (GPU 32/36). Clean chain, no API: contract → contract-driven plan (spec's real files, NOT the generic section templates) → per-component isolated build → verbatim slotting + deterministic className enforcement → **SpecVerifier 10/10 birebir · Tailwind v4 · all named files**. Proof: every one of the 11 exact literals present verbatim, no raw `__SLOT__`, esbuild 0 errors, **`vite build` succeeds with real Tailwind v4 CSS (13.96 kB)**, and the headless render shows the faithful dark portfolio (NexoraAI logo, exact nav links, "NexoraAI Portfolio" title + exact subtitle, grayscale Unsplash hero, amber accent). Decisive lessons that turned 5/10 → 10/10: (a) plan must come from `contract.fileArchitecture`, not `deriveSectionPlan` (which hallucinated a restaurant "Hizmetler/İletişim"); (b) each component turn must be **history-isolated** (`options.isolate`) or the model clones the previous file (Hero=Navbar copy); (c) className fidelity needs **deterministic tag-based enforcement** — the 3B rewrites the outermost element's class no matter how the prompt begs; (d) `postGenVerify`'s heuristic repair is fidelity-hostile (reverted v4, added framer-motion) so it's skipped for fidelity builds — the SpecVerifier IS the contract.

**Mission:** NexoraAI reliably executes only prompts written for its own conventions. The first paste of a hyper-detailed *external* brief (a Gemini-authored dark portfolio: Tailwind **v4** with `@theme`/`oklch`, a named multi-file component architecture, ~8 EXACT Turkish copy strings, ~5 EXACT Unsplash URLs, specific class strings on named elements) failed on *every* local model — and the honest post-mortem is that ~80% of that is **self-inflicted**, not a model ceiling. `scaffoldProject()` and the size-based profiles COERCE every build into React+Vite+**Tailwind-v3**+one-file, structurally destroying fidelity to the spec. The 1→10 leap is a deliberate inversion, straight from how Bolt/StackBlitz-WebContainers already get "any stack" right: **stop dictating the stack — treat the spec (and the model-authored `package.json`) as ground truth**, and only reach for a bigger model when a deterministic check proves the local one genuinely cannot. This is a **FIDELITY MODE** that pre-selects on a specificity score (and is a visible toggle), leaving today's opinionated creative scaffold byte-identical for normal prompts.

- [x] **9.1 The Project Contract — parse the spec once into machine-readable ground truth.** A dependency-free `SpecExtractor` (new `electron/shared/projectContract.ts`) turns the incoming prompt into ONE Contract: `{ tailwindVersion, pinnedDeps, fileArchitecture[], slots(exact copy/URLs/class-strings), imageUrls[], colorTokens[] }` via deterministic heuristics (quoted strings, `http(s)` URLs, `class(Name)="…"` literals, `@import "tailwindcss"`/`@theme` markers, `pkg@^x` pins, bulleted file paths) + a **specificity score** that pre-selects Fidelity Mode. Computed in `llamaService.ts` right after `detectProfile`, persisted beside the `Projects/<slug>/knowledge/` store so every turn AND the Debug Engine read the same truth. *(Guard: require ≥2 hard signals so a vague prompt keeps the creative scaffold; Fidelity Mode is user-visible, not a silent override.)*
- [x] **9.2 Contract-obeying scaffold — Tailwind v3/v4 branch + model-authored `package.json` is authoritative.** `scaffoldProject()` (`agentService.ts:132`) is hard-wired to v3 (`usesTailwind` only sees `@tailwind`; always `tailwindcss ^3.4.6` + postcss; always injects `tailwind.config.js`/`postcss.config.js`; REPLACES `pj.dependencies` from `KNOWN_VERSIONS`). Add `detectTailwindVersion()` (v4 markers `@import "tailwindcss"`/`@theme`/`@utility`/`@source` → `@tailwindcss/vite` + `@import "tailwindcss"`, NO config files; else v3 unchanged). Add **faithful mode**: when the model emitted its own `package.json`, VALIDATE-don't-overwrite (keep versions verbatim, repair only provably-broken specifiers, keep the ETARGET guard) — the `KNOWN_VERSIONS` wholesale rebuild stays only for greenfield/no-manifest. This one branch is why a v4 spec fails today. *(New v3+v4 fixtures in `tests/scaffold.mjs`.)*
- [x] **9.3 Verbatim slotting + a spec-driven fidelity profile — copy, don't paraphrase; plan-first regardless of size.** Take exact literals OUT of the generation path: replace `contract.slots` with opaque `__SLOT_N__` tokens in the requirements, the model emits STRUCTURE with tokens, a deterministic `rehydrate()` swaps them back on disk after generation (repairs run on the still-slotted source so literals stay intact). New `FIDELITY_REACT` branch in `buildSystemPrompt()` renders the Contract as a fenced spec + "follow it to the letter, emit `__SLOT_N__` for all copy/URLs/classes" + a tiny verbatim exemplar. Route high-specificity prompts to **plan-first-per-file regardless of model size** (the compact one-file squeeze is itself a fidelity killer), threading each file's slot subset into its `expectFile` turn; keep plan reasoning grammar-free to avoid the sub-13B "constraint tax".
- [x] **9.4 The deterministic SpecVerifier — fidelity is value-accuracy, not build-green.** Grammar guarantees format, never content (models pass JSON-shape ≫ value-accuracy). A no-model `SpecVerifier` runs after rehydrate+scaffold and asserts per-requirement: every slot's exact bytes present verbatim, every image URL intact on its element, every required class string on its target, the INSTALLED Tailwind version == contract, every named file exists → a **Fidelity Score**; gate "verified" on `fidelityScore == 1.0` for pinned literals AND build-pass (not build-pass alone, fixing the Faz 8.4 "verified-without-the-line" hole), and feed ONLY the failed requirements back as the next hunk-scoped repair.
- [x] **9.5 Verifier-gated escalation — climb, don't loop.** Today the Debug Engine retries the SAME local model on failure → a spec beyond the 3B/8B ceiling burns turns forever. Make the SpecVerifier fail-list the cascade gate: on repeated fidelity failure for the same task, ESCALATE the generation step (local tier bump 8B→14B, then the already-wired frontier API in `apiEngine.ts`) instead of an identical retry — while VERIFICATION stays cheap+local (a 3B can grep "does this string appear?"). A predictive pre-router (specificity + exact-string density + non-default stack, bootstrapped from Motor Karnesi stats) escalates genuinely-hard specs UP-FRONT. Depth-capped; every escalation logs app-fault-vs-model-ceiling.
- [x] **9.6 Sandbox + real-run asset parity — v4 preview, import-map deps, remote images.** The in-app sandbox (`Preview.tsx`) hand-stubs libraries + inlines a v3 Tailwind CDN, so a correct v4/asset spec previews wrong. Detect v4 → swap to `@tailwindcss/browser@4`; add an ONLINE mode injecting a `<script type=importmap>` from the union of the model's imports (esm.sh, `?external=react` to avoid the two-Reacts hook crash), keeping the vendored stubs as the OFFLINE fallback; inject a CSP `<meta img-src * ; font-src *>` + `no-referrer` so Unsplash/Google Fonts render while `script-src` stays locked; a capture-phase `img.onerror`/unknown-icon → placeholder so a dead URL never blanks the render. On the real build, broaden `KNOWN_VERSIONS` to auto-add ANY imported icon/font/util lib (heroicons, react-icons, radix, recharts…) instead of dropping it.

**Sequencing:** ship **9.1+9.2** together (contract + the Tailwind v3/v4 branch — the single highest-impact unblock), then **9.3** (small-model verbatim fidelity), then **9.4** (the trustworthy fail signal), then **9.6** (preview parity, runs parallel), then **9.5** (escalation, cheap because the API path already exists). **The decisive success test:** paste the Gemini portfolio prompt in Fidelity Mode against a LOCAL model — PASS requires (a) scaffold emits no v3 config, installs `tailwindcss ^4` + `@tailwindcss/vite`; (b) "Çalıştır" runs the project's own dev script with ZERO Debug-Engine build failures; (c) `fidelityScore == 1.0` (every Turkish string, Unsplash URL and class string byte-for-byte present, grep-verified); (d) the sandbox renders v4 styling + real images; (e) any 8B miss triggers ONE verifier-gated escalation that then re-passes. Regression guard: the same prompt in normal creative mode leaves the v3 scaffold byte-identical and `test:engine` green.

## Phase 10 — Ecosystem & Discoverability (v0.16.x) — FROM COLLEAGUE TO PLATFORM ✅ SHIPPED

> **✅ Shipped (v0.16.0, 2026-07-09).** All 10.1–10.9 delivered + live-tested; MCP client, local serve endpoint, ⌘K palette, checkpoints/rewind, notifications/tray, global search, scheduled tasks, slash-workflows, and the 157-provider hub are all in the app.
>
> **Planned (2026-07-09).** Chosen from a 7-agent competitor sweep (real Codex desktop screenshots as ground truth + web research on Codex CLI/cloud, Cursor/Windsurf, Cline/Roo/Continue/Aider/OpenHands, LM Studio/Jan/Ollama/AnythingLLM, and cross-cutting desktop agent-UX). Verdict: NexoraAI's engine, agent-safety, repair-verify and fidelity layers already lead the field (deeper than most rivals); the real remaining gap is **ecosystem interop + discoverability + long-run UX** — all achievable WITHOUT breaking local-first.

**Mission:** NexoraAI out-engineers most competitors on safety/repair/fidelity, but it re-implements every tool by hand, can't be *driven by* other editors, and lacks the keyboard/checkpoint/notification polish that makes minutes-long local runs pleasant and auto-approve genuinely safe. Phase 10 closes that — **every item is local-first ("data never leaves the machine"); nothing here uploads a repo or needs a cloud VM.** Cloud-dependent competitor features are deliberately excluded (see the guard below).

- [ ] **10.1 MCP client — local stdio tool servers (highest ecosystem lever).** Add a Model-Context-Protocol client so NexoraAI can load local `stdio` MCP servers (filesystem, git, sqlite, browser, docs…) and expose their tools to the agent — instead of hand-writing every capability. It's the whole ecosystem's standard (Codex, Cursor, Cline, Roo, Continue, LM Studio, Jan, Claude Desktop, AnythingLLM, Msty all speak it), and `stdio` MCP runs entirely on-device — no cloud. Tools route through the existing **trust tiers** (`electron/shared/trust.ts`) and permission modal, so an MCP tool call is gated exactly like a `[RUN]`. A server registry lives beside the knowledge/rules store.
- [ ] **10.2 Serve our own engine — a local OpenAI-compatible HTTP endpoint.** Today `apiEngine.ts` only *consumes* an outbound API; NexoraAI never *serves* its engine. Expose the local worker as an OpenAI-compatible `/v1/chat/completions` (opt-in, `127.0.0.1`, off by default) so the same local model can power **both** the NexoraAI UI **and** external editors (Continue.dev, Cline, VS Code, Zed). Huge interop/reach win with zero privacy cost — the request never leaves the machine.
- [ ] **10.3 Command palette (Ctrl/Cmd+K) — every action fuzzy-searchable.** NexoraAI's controls are scattered (Scan, Run, Review, model select, settings, theme, open session, restore-green…). One fuzzy launcher collects them so power users work without the mouse and discoverability jumps. Pure client-side, zero backend. (Cursor, Codex, Zed, VS Code, Raycast all have it.)
- [ ] **10.4 Per-prompt checkpoints + inline rewind (code / chat / both).** We already have git-truth restore (restore-green, restore-to-hash, Stop-snapshot, undo/redo timeline) — but not an inline checkpoint on *every prompt* in the chat timeline, nor a "revert code only / chat only / both" axis (Claude Code `/rewind`, Cline, Cursor). Cline's proven principle: checkpoints make auto-approve safe — one click undoes a bad turn, which directly emboldens our Auto / Full-Access tiers. Pure local file snapshots on the existing snapshot machinery.
- [ ] **10.5 Native notifications + `powerSaveBlocker` + tray.** Turns run for *minutes* (verify/repair chains, 3B builds); when the user leaves the window they miss turn-done / build-broke / queue-stuck, and the machine can sleep mid-run. Electron OS notifications + `powerSaveBlocker` + a tray presence fix long-run UX — hooked to the single existing `logRepair` chokepoint, so near-zero new instrumentation.
- [ ] **10.6 Global search — full-text across sessions / projects / knowledge (+ git branch).** Sessions live in `~/NexoraAI/Sessions` and knowledge in `*.md`, but the sidebar has no global search, so past decisions/diffs/walkthroughs/repairs can't be re-found. Local content + branch fuzzy index; makes crash-recovery return-to-work (a known NexoraAI failure mode) painless. (Codex global thread search, ChatGPT Cmd+K history.)
- [ ] **10.7 Scheduled / recurring local tasks (local cron + jitter).** "Every morning scan the repo for regressions", "nightly update+test deps". The task queue already exists; add a local cron cadence with jitter (so heavy 3B runs don't stack). Runs in the project's own dir — no cloud. (Codex Automations, Claude Code scheduled tasks.)
- [ ] **10.8 Slash-command workflows (.md → `/command`) + approved-memory UX.** The knowledge/rules core exists; add (a) packaging a multi-step recipe from a `.md` file into a `/slash` command (Windsurf Workflows, Cline workflows folder) and (b) a "remember this" + "suggested → user-approves" memory UX (Cursor/Windsurf Memories). Both sit on the existing deterministic-learning core; zero cloud, low effort, high daily payoff.
- [ ] **10.9 Provider hub — opt-in BYO-key, the COMPLETE provider catalog (most-requested; full parity with OpenCode, nothing curated-out).** Today `apiEngine.ts` is a *single* OpenAI-compatible endpoint (opt-in, off by default, used for Faz 9.5 escalation). Generalize it into a **full provider registry that shows EVERY provider an OpenCode/LM-Studio-class app shows — the whole list, not a hand-picked few** — sourced from an open catalog (the same **models.dev**-style dataset OpenCode uses: per-provider base URL, auth style, model list), **bundled offline for the entire list + an optional one-tap refresh**. Dozens of providers with search + a "show more" (OpenAI, Anthropic, Google/Gemini, OpenRouter, DeepSeek, Groq, Mistral, Together, Fireworks, xAI, Cerebras, Deep Infra, Databricks, DigitalOcean, Vercel AI Gateway, Azure OpenAI, AWS Bedrock, FastRouter, CrofAI, … + **Custom** base-URL). The **vast majority speak the OpenAI-compatible API**, so they light up through the existing transport with just a base-URL preset pulled from the catalog — that's *why* the list can be complete cheaply; the handful of native formats (**Anthropic, Google**) get small adapters, and gateways (OpenRouter / Vercel AI Gateway) cover the long tail via one key. Per-provider **API key in OS keychain (`safeStorage`)**, **model-list fetch + model picker**, wired into the hybrid modes (`off`/`fix`/`all`) + verifier-gated escalation. **Local-first stance preserved:** the local GGUF model stays the default and first-class; every provider is strictly **opt-in**, off by default, each labeled "using this sends your prompt to \<provider\>"; the catalog is static bundled data (showing the full list needs no network) and the repo is never uploaded — BYO-key reach, *not* silent cloud offload (contrast the OUT list). Highest external-demand item.

**Backlog (evaluated, deferred to 10.10+):** in-app **git commit / push** from the turn's diff (only PR-creation is cloud-bound → partial); **in-browser click-to-comment** on the live preview (extends the Faz 7.4 screenshot-anchored comments); **JSON-Schema grammar-constrained decoding** (node-llama-cpp grammar — hardens SEARCH/REPLACE/plan/verdict for weak 3Bs); **repo-map / Codemap** for @-mention context (curbs the "invented file" failure); **isolated-worktree** risky turns (extends `options.isolate`); **local Whisper.cpp dictation** (API-free voice-to-prompt). **Tab / next-edit inline completion** is noted but the hardest — it needs a dedicated low-latency completion model the current 3B/14B build models can't be. **Model idle-TTL/auto-evict + speculative decoding** (reclaims RAM/VRAM against the >4GB Electron cage; runtime lifecycle on top of the existing Hardware Advisor).

**Explicitly OUT — local-first guard:** Codex Cloud async fire-and-forget, cloud/background agents that **upload the repo**, cloud-VM PR generation, thread local↔remote handoff, Slack/Linear/GitHub @-mention task triggers, Computer Use (native-app control), cloud image generation, live web-fetch. These break "data never leaves your machine" — NexoraAI's entire reason to exist. Only the **async/queue UX *pattern*** is borrowed as inspiration (already approximated by the local task queue).

**Sequencing & success test:** ship **10.1 + 10.2** first (highest strategic leverage — ecosystem + interop), with **10.9** (the provider hub, its API-layer sibling) pulled up next to them since it's the highest *external* demand; then the daily-use polish in order (10.3 palette → 10.4 checkpoints → 10.5 notifications → 10.6 search → 10.7 schedule → 10.8 workflows). PASS: (a) a local stdio MCP server's tools appear and are callable in a turn, gated by the existing trust tiers; (b) Continue.dev/Cline points at NexoraAI's `127.0.0.1` endpoint and drives the same local model; (c) Ctrl+K runs every major action; (d) every prompt has a one-click rewind (code / chat / both); (e) a finished/failed long run raises an OS notification and the machine doesn't sleep mid-run. Every sub-phase live-tested on the real app before its tick, and `test:engine` stays green.

---

## Phase 11 — The API Unleashed & The Creative Workspace (v0.17.x – v0.18.x) ✅ SHIPPED

> **✅ Shipped (v0.17.0 → v0.18.2, 2026-07-09/10).** Live-testing the finished ecosystem layer with a real cloud API model exposed a deeper truth — the whole pipeline was tuned for a weak 3B and those crutches were *caging capable models*. Phase 11 cut that cord and then turned NexoraAI into a full creative workspace.

- [x] **11.1 The API Unleashed (v0.17).** Full conversation history + depth on the API path; a one-shot **frontier build/edit path** (elite persona, no sectioning, no 2 K caps) for any **strong model — API *or* ≥ 13B GGUF**; **complete removal of the surgical-edit machinery** (forced `SEARCH/REPLACE`, oversized-SEARCH stream cutter, whole-file-rewrite ban) that helped no model and aborted API turns. Plus clean dev-server shutdown, an accessibility UI-scale control, and an **Open Project** entry.
- [x] **11.2 Vision routing (v0.17.1–.3).** Attaching a reference image no longer forces the local Qwen-VL: on an API model the image goes straight to the API as multimodal; a conservative `isVisionCapableModel` **warns** on text-only models instead of hallucinating; and image→project became a **two-stage flow** (API analyzes the image into a measurable spec → API builds the complete project from it). Local VL downloads a **device-appropriate** size.
- [x] **11.3 Image generation — any image model, in the chat (v0.18).** Detect image models (`image`-token heuristic + families: z-image, qwen-image, dall-e, flux, gpt-image, imagen, seedream…) and route to the right endpoint — **OpenAI-compatible** `/images/generations` *and* a **native DashScope adapter** for Qwen-Image. In-chat **preview · fullscreen · download · add-to-assets** per image, plus **aspect ratio, 1–4 variations, negative prompt, image-to-image, exact-prompt fidelity**, and a composer hint. Add-to-assets decodes to a **real binary** in `src/assets/` and the Preview resolves the import.
- [x] **11.4 Full file & shell access from chat (v0.18).** Plain-language file ops — *"convert to WebP with Pillow / delete / rename / copy"* — emit a shell command that runs in the **project folder** through the existing **trust tiers** and syncs the result back into the editor (new `.webp` appears, deleted file disappears). Identical on local and API.
- [x] **11.5 Workspace polish (v0.18).** **Free scrolling** while generating (stream no longer yanks to the bottom); **project-identity** guarantee (every new build writes a `package.json` → its own clean workspace dir, so file-ops target the right place); **generic, user-selectable local vision model** — any VL GGUF (Qwen3-VL, LLaVA, MiniCPM-V, InternVL…) you drop in the models folder is detected and picked from Settings, no longer hardcoded to Qwen2.5-VL.
- [x] **11.6 Two-pane Settings (v0.18.1).** Settings had grown into one long scroll. It's now a **two-pane layout** (Claude/ChatGPT-style): a **left category nav** — General · Providers · Models · Trust & Permissions · Prompt & Rules · Knowledge · Tools · Commands · Engine — and a **right content pane** showing only the selected category, always opening on General. Each section is gated by its category (`hidden` class / `&&`) so the underlying feature logic is untouched. The user-selectable local vision model (11.5) now lives under the **Models** category.
- [x] **11.7 Settings UX hardening (v0.18.2).** Two navigation annoyances fixed. **(a)** The sidebar's bulky **"Theme & Language"** box was removed; theme (Light/Dark) + language (TR/EN) now live in **Settings → General → Appearance** as a segmented control at the top of the section. **(b)** The **Provider Hub** went **master-detail**: instead of expanding the selected provider's config as an accordion at the *bottom* of the 157-item list (forcing a scroll to the floor), clicking a provider now opens its own focused config pane **at the top** with a `‹ All providers` back button — key, model id, fetch-models and the enable/disable list all there. `detailOpen` is UI-only state, so returning to the list keeps the active provider selected.

---

## Phase 12 — The Universal Agent & the Multilingual Interface (v0.18.3) ✅ SHIPPED

> **Shipped v0.18.3 (2026-07-10).** Two threads that make NexoraAI *universal*: the agent that actually **does** what you say on your computer (in any wording, any language, checking reality first), and an interface that speaks **10 languages** in each one's natural direction.

- [x] **12.1 Full computer access from chat — execute, don't explain.** From chat, EVERY model (API + local, any provider) actually performs what you ask on the machine via the terminal (`[RUN]`) and reports the REAL result — check if a tool is installed, run a command, open an app, inspect files — instead of describing how you could do it or writing snippet files. *Root cause was 4-layer: action-intent messages were routed to the code/build persona (dumped files); `detectAgentIntent` only matched install/download; the AGENT_HINT gate excluded every chat turn; and the chat persona forbade actions. All four fixed. Live: `qwen-plus` "vercel yüklü mü kontrol et" → ran `command -v vercel` → "Vercel CLI kurulu değil"; "google chrome aç" → launched Chrome.*
- [x] **12.2 Intent over keywords.** The capability is model-driven, not a regex allowlist: a `COMPUTER_ACCESS_GRANT` in every persona (chat + frontier build/edit + capable local) tells the model to understand the user's INTENT regardless of phrasing — short or long, with or without a magic word. *Also fixed a Turkish `\b`-boundary bug (ASCII word boundaries break on `ı/ç/ş/ö/ü/ğ`) by moving the intent regexes to Unicode `\p{L}` lookarounds. Live-verified keyword-free ("masaüstümde neler var" → `ls ~/Masaüstü`).*
- [x] **12.3 Check → Understand → Do.** Real ENVIRONMENT facts are injected each relevant turn (resolved live: home, the actual localized Desktop/Downloads via `xdg-user-dir`, OS, shell, locale) so the model never guesses paths (Turkish Desktop is `~/Masaüstü`, not `~/Desktop`). When something isn't given, the grant tells it to run a quick **check** command first, read the real result, **then** act. Live: it used `/home/mutlu/Masaüstü` and returned the true listing.
- [x] **12.4 Reply in the user's language — any language.** The reply language now follows the language of the user's message (detected by the model), not the app's UI setting. English→English, German→German, French→French, Turkish→Turkish… Live-verified on `qwen-plus` across DE/FR/ES/EN.
- [x] **12.5 10-language UI with per-language direction.** Settings → General → Appearance has a **10-language picker** (🇹🇷 🇬🇧 🇪🇸 🇫🇷 🇩🇪 🇵🇹 🇷🇺 🇨🇳 🇯🇵 🇸🇦) and the whole interface renders in the chosen language — **each in its natural direction**: LTR for most, **RTL for Arabic** (the entire UI mirrors via `<html dir="rtl">`). *~256 UI strings translated by 8 parallel localizer subagents (one per language); a source-string-keyed `tt()` layer + expanded `translations.ts`; `Lang` type + `applyLangDir`. Live-verified: ES/JA/RU render natively, AR flips the Settings modal fully RTL.*

---

## Phase 13 — Local Image Generation (offline, on-device) — THE FINAL LOCAL-FIRST GAP ✅ SHIPPED (v0.19)

> **✅ SHIPPED 2026-07-11 as v0.19.0.** What actually landed (all live-verified on the 4 GB RTX 2050, judged from on-disk session files, locked by new test suites):
>
> - **Core offline engine** — `localImageService.ts` sidecar (`sd-server`, port 8092 → `AVOID_PORTS`, flat-zip binary path, POSIX group-kill); `generateImage()` routes local **before** `active()`; SD-friendly `localImageSize()` (the 1024² OOM → "no results" class fixed). Real PNGs offline in ~15–30 s.
> - **Free model choice, GGUF-style** — in-app device-fit catalog (🟢/🔵 VRAM badges, one-click download) **+ free HuggingFace search** (any single-file `.gguf`/`.safetensors`; mmproj/VAE/LoRA/diffusers components filtered). Lives in the **chat-screen Model Browser (Text/Image toggle)**; the composer picker switches **local-text ↔ API ↔ local-image** freely, selected model threads through `preferLocal`/`localModelPath`.
> - **Faithful any-language prompts** — non-ASCII prompts translated on-device by the loaded LLM (isolated one-shot, `<think>` stripped) under a fidelity contract (*turuncu=orange* lock; no invented weather/clouds); SD quality boosters appended.
> - **Shared conversation memory** — `noteExchange`/`seedHistory` on BOTH engines (server + worker parity): image turns enter local engine history; `[Görsel üretildi]` markers + a deterministic **context digest** in API history (no more silent truncation); model switches and session opens **seed** the new engine — the chat is never wiped.
> - **Assets pipeline** — add-to-assets from the card **and from chat** (`[ASSET]`, add ≠ regenerate locked, no unsolicited adds); `composeTurnPrompt` pure function sends the asset list **unconditionally** (the picsum-invention bug) — `test:assetctx` 55 checks.
> - **13.8 as intent, not syntax** — `[IMG]`/`[ASSET]` directives: ANY model (local text or API), ANY language, delegates image intent to SD; plus the **`[BUILD]` intent bridge** + `ANSWER:` lock — routing heuristics demoted to performance hints, the model overrides in both directions (MIXED MESSAGES rule: instructions can't be swallowed).
> - **i18n** — the new image UI + catalog: 28 strings × 10 languages, RTL Arabic verified live.
> - **New suites:** `test:assetctx` (55) · `test:imgdir` (29) · `test:intentbridge` (13) · `test:apihistory` grown to 25 — engine chain now 31 suites.
>
> **Deferred to backlog (unblocked, not needed for v1):** 12.3 native seed/steps endpoint, 12.4 explicit VRAM mutex (CPU-LLM + GPU-SD split held up fine live), 12.5 CI binary vendoring/bundling (binary auto-downloads on first use), 12.7 gray-output validator, 12.9 spawn ladder extras, TAESD preview / img2img / LoRA.

> *(Original plan below, kept for the record.)* **Planned as v0.19.** Redesigned 2026-07-10 from a 12-agent research + adversarial-verification sweep. v0.18 shipped image generation on the **API**; the local-first mission demands the **offline** twin. NexoraAI already runs local **LLMs** (llama-server) and local **vision INPUT** (VL sidecar on 8091) — the one thing it can't do on-device is **generate** images. This phase closes that. **The core was proven end-to-end during research: `sd-server` spawned on the real 4 GB RTX 2050, internet off, no API key → a real 457 KB PNG in ~33 s.** So this is de-risked; the work is careful engineering, not discovery.

**Engine decision (made):** **`leejet/stable-diffusion.cpp` `sd-server`** — the "llama.cpp of diffusion." Same GGML/GGUF lineage; a spawnable HTTP server that holds **all weights out-of-process** (satisfies our >4 GB-V8-crash rule by construction, exactly like the 8091 vision sidecar); MIT binary; tiny prebuilt per-OS zips (Linux Vulkan 42.7 MB / Win Vulkan 35.9 MB / macOS Metal 46.9 MB); **no Python, no Node addon.** Its `POST /v1/images/generations` returns `{data:[{b64_json}]}` — the byte-for-byte shape our `generateImageOpenAI()` already POSTs → point the base URL at `127.0.0.1:8092` and **reuse the entire shipped in-chat UX** (preview · fullscreen · download · add-to-assets). Fallback = the **CPU rung of the same binary** on a GPU→CPU spawn ladder (not a different engine). Pin `SD_BIN_TAG=master-769-cc73429`.

**Model tiers (one binary, quality = a different GGUF gated by the Advisor):**

| Tier | Model | Disk (total) | VRAM @512² | Steps | Measured speed | License | Badge |
|---|---|---|---|---|---|---|---|
| **v1 — SHIP FIRST** | **SD 1.5 Q4_0** (1 file, VAE baked in) | **1.57 GB** | ~1.5 GB | 20 | **~33 s** RTX 2050 / minutes CPU | OpenRAIL-M (commercial OK) | 🟢 fits 4 GB |
| v1 fast | SD 1.5 + LCM/Hyper-SD LoRA | 1.57 GB+ | ~1.5 GB | 2–4 | ~7–10 s GPU (est.) | OpenRAIL-M | 🟢 fits |
| high (clean license) | Flux.1-schnell Q4_0 (**4-file bundle ~16 GB total**) | ~16 GB | ~7–8 GB | 4 | ~10 s+ (8 GB) | **Apache-2.0** | 🔵 spills |
| high (scale) | Z-Image-Turbo | ~Q4_0 + encoders | 4 GB via offload | 8 | ~2–3 s (4090) | Apache-2.0 | 🔵 spills |

**👉 Ship/download FIRST: SD 1.5 GGUF Q4_0 (1.57 GB, `second-state/stable-diffusion-v1-5-GGUF`)** — single file, VAE baked in, fits 4 GB, runs on CPU, commercial-safe. **Never default SDXL-Turbo/SD-Turbo (Non-Commercial) or Flux-dev (NC)** — opt-in with terms shown. Advisor must display the **total** multi-file size for Flux (~16 GB, not the 6.77 GB diffusion file alone).

**Integration architecture:** a new **`electron/main/localImageService.ts`** ≈ a corrected clone of `visionService.ts` — `IMAGE_PORT 8092` (add to `AVOID_PORTS`), reuse `downloadFile()` (%-to-chat), the stdout readiness scan (`/listening|HTTP server/i`), the `LD_LIBRARY_PATH=dirname(bin)` spawn env, POSIX group-kill. Exports `ensureLocalImageReady` (resolves a model path **before** spawn), `startImageServer(modelPath)`, `stopImageServer`, `hasLocalImageModel`, `generateImageLocal(prompt,opts)`. A ~10-line local branch at the **very top** of `generateImage()` (before `active()`) routes to it; everything downstream (the `IMAGE_GENERATE` handler, renderer preview/download/assets, `ImageGenOptions`) is untouched.

**Sub-phases (order corrected by the adversarial pass):**
- **12.1 Model-first spawn** — `ensureLocalImageReady()` guarantees a hand-placed SD1.5 GGUF, THEN `startImageServer(modelPath)` spawns `sd-server -m <gguf> --listen-ip 127.0.0.1 --listen-port 8092`; 8092 → `AVOID_PORTS`; **explicit flat-zip binary path** (sd.cpp zips are flat, not nested like llama — don't clone vision's nested `serverBinaryPath()`); `chmod +x`. *Accept:* internet OFF, `curl …/v1/images/generations` returns `b64_json`.
- **12.2 🎯 FIRST MILESTONE — real PNG in chat, offline** — local branch in `generateImage()` + the existing `IMAGE_GENERATE` path; widen the renderer `isImageGenModel` gate + add an `activeLocalImageModel` flag. *Accept:* internet OFF + no API key → "a red panda coding" → inline PNG via the shipped UX; add-to-assets writes a real binary to `src/assets/`. **Ship a spinner+elapsed from the FIRST request (33 s is not instant).**
- **12.3 Per-request control (pulled forward)** — seed + steps + sampler + CFG via the native `/sdcpp/v1/img_gen` family (or `<sd_cpp_extra_args>` embedded in the OpenAI prompt), because the reused OpenAI endpoint alone gives **zero** per-request control and seed is the highest-value knob for the iterate-loop.
- **12.4 LLM↔SD VRAM handoff** — an explicit mutex on 4 GB: pause/`--offload-to-cpu` the LLM around an image gen, or run SD on CPU while the GPU serves the LLM. Non-optional; an `[IMG]` mid-build would otherwise OOM one server.
- **12.5 CI vendoring + bundle** — per-job `vendor/sd-bin` (Vulkan Win/Linux, Metal macOS, CPU) mirroring `vendor/node-bin`; **bundle the default Vulkan binary in app resources** (not curl-at-runtime — limits AV/Gatekeeper friction); CUDA (924 MB) on-demand only. Installers grow ~+40 MB/platform.
- **12.6 Model download + first-run + Advisor tier** — auto-download SD1.5 Q4_0 with %/cancel + SHA-256 pin + refuse `.ckpt/.pt/.pth` (accept only `.safetensors`/`.gguf`); extend the VRAM Advisor with an image tier table (show **total** size); off/local/API toggle.
- **12.7 Correctness validator** — reject the #1220 silent-gray VAE-OOM output (channel-variance/uniform check on the decoded PNG + stderr scan for `ErrorOutOfDeviceMemory`); **never trust the exit code**; auto-retry lower-res/more-offload.
- **12.8 `[IMG]` agent directive** — `[IMG] a minimalist fox logo -> src/assets/logo.png [--seed 357925 --ar 1:1]` calls `IMAGE_GENERATE` and writes bytes to `src/assets/` (explicit wiring — NOT free reuse of add-to-assets), trust-gated, post-directive rescan. Deterministic seeds for byte-identical rebuilds (same-machine).
- **12.9 Spawn ladder on CURRENT flags + advanced controls** — `--backend vulkan`/`--max-vram -1`/`--offload-to-cpu`/`--diffusion-fa`/`--vae-conv-direct` (the cited `--clip-on-cpu`/`--vae-on-cpu` are **deprecated**); TAESD live preview; img2img strength / LoRA / upscale.
- **12.10 `test:imagelocal` + live verify** — lock binary-URL/routing/flat-zip-path/scan/badges; `test:engine` green; a real offline PNG on the 4 GB card.

**Top risks (with mitigations):** out-of-process/OOM → own process + idle-unload + serialize vs LLM; silent gray corruption → post-gen validator; licensing → Apache/OpenRAIL defaults only, NC opt-in, pass-through notice as a build task; NSFW → tame SD1.5 base, on-device, don't market "uncensored"; weight provenance → SHA-256 pin + `.safetensors`/`.gguf` only; unsigned-binary AV → bundle default binary + ship checksums. **World-first (hedged):** "first agent-first, in-installer, fully-offline image generation via a directive" — Replit is cloud/metered, LM Studio bridges to external SD via MCP; verify vs current Cursor/Windsurf/Antigravity before the README line.

**Full detail, all findings, and the corrected file-touch map → [`ROADMAP-LOCAL-IMAGE-GEN.md`](ROADMAP-LOCAL-IMAGE-GEN.md).**

---

## Phase 14 — "Intent Brain": retrieval, fidelity, and the ask-before-you-build loop (v0.20) — ✅ SHIPPED

> **✅ Shipped as v0.20 (2026-07-12).** All 10 sub-phases built + live-verified (repo-map, `[SEARCH]`/`[SYMBOL]`, semantic index, triggered grammar, Intent Gate, AGENTS.md interop, KV-slot resume, afterEdit, `[EDIT]` img2img, behavior check), each with a dedicated test suite (`test:engine` = 57 suites green). An adversarial 10-agent audit of the new suites then caught three integration gaps (all fixed): the `[EDIT]` fidelity/subject bug, a `Map`-access bug that silenced the full-rewrite warning, and a `href=""` behavior blind spot. **Two mid-phase decisions reshaped the engine:** (a) the owner removed the **deterministic "tool" repair layer (rung 0)** entirely — every fix now goes to the model (multi-file-aware, identity-pinned regenerate; the duplicate-import build that used to roll back now fixes in one round); (b) **history parity** — a strong local model's seed carryover now scales to its context window instead of a flat 12 k, proven byte-identical to the API path. Two follow-ups deliberately deferred (see below): the 14.7 speculative-decoding *draft* toggle (KV-slot half is live) and wiring the 14.4 GBNF grammar to the server (repair-turn fidelity is live).
>
> **Planned as v0.20.** Designed 2026-07-11 from a **10-agent live internet research sweep** (Cursor, Windsurf/Devin, Copilot, Cline/Roo/Aider, Bolt/v0/Lovable/Replit, Antigravity/Kiro/Spec-Kit, the local-runtime ecosystem + the context-engineering literature) + adversarial synthesis. NexoraAI is already the category leader on its own axes (local generation, debug/repro engine, agent layer, 157 providers, local vision-in + image-gen, Universal Agent with intent directives). The sweep found the real gaps are **not more surface area** but three structural levers — and every item below stays **100% on-device** and **intent-based**:
>
> 1. **It never checks whether it understood the intent before spending minutes building, and it can't retrieve the RIGHT context on real repos** — today it dumps whole files with a silent 400-file / 200 KB cliff (no repo-map, no embeddings, no symbol nav). This is the biggest correctness+cost lever and the thing that makes NexoraAI usable on *existing* repos, not just greenfield.
> 2. **It under-exploits the very engine it already spawns** — `llama-server` supports speculative decoding, KV-slot cache, `--embedding`, and lazy/triggered grammars; source confirms **none are wired**. Free on the table: **1.4–2.5× generation speed**, **~93% faster session/project resume**, real local retrieval, and **~100% parseable directive-JSON on a 3B**.
> 3. **Its intent directives are free-form text a 3B mangles, and the `sd-server` it already runs exposes img2img/inpaint/edit that `localImageService` never calls** — plus the live VL sidecar can OCR for free.

**Sub-phases (build order — all on-device, all intent-native):**
- **14.1 Offline repo-map** *(FIRST — retrieval foundation)* — tree-sitter def/ref symbol graph + **personalized PageRank** signature skeleton (message-mentioned files ranked 10×, in-chat files 50×), incremental reparse-on-save, injected via the existing per-turn budget path. **Replaces the whole-file dump** with "retrieve the ~30 k tokens that matter." *Intent:* the model picks the query; PageRank is personalized by the current message so retrieval tracks intent turn-by-turn.
- **14.2 `[SEARCH]` + `[SYMBOL]` intent directives** — model-emitted hybrid retrieval (ripgrep lexical + ast-grep structural + optional semantic, layer preference stated in plain language = hint not regex), merged into a token-budgeted block; **symbol-level surgical edits** (`find_symbol` / `find_referencing_symbols` / `go_to_definition` / `replace_symbol_body`) gated by the existing per-hunk diff review.
- **14.3 Offline GGUF code-embedding index** — serve `nomic-embed-code` / `jina-code-1.5b` via `llama-server --embedding`, on-disk vectors, **Merkle-hash incremental re-index**, cAST function-boundary chunking → feeds the semantic layer of `[SEARCH]`; `@-file` / `@-symbol` / `@-folder` pinning with a context-chips bar + token cost + "what got dropped and why."
- **14.4 Triggered-grammar directive & tool-arg fidelity** *(small, foundational)* — extend `editGrammar.ts` to a **lazy/triggered GBNF** that constrains `[RUN]`/`[IMG]`/`[BUILD]`/`[MCP]`/tool payloads to a valid schema **the instant the model decides to act**; one hidden repair turn only on *semantic* failure; tool-template health badge on model load. Makes every intent directive ~100% parseable on a 3B.
- **14.5 Intent Gate + Interpretation Fork** — a calibrated **underspecification pass** before any byte is written: proceed on clear tasks (near-zero overhead), else ask the *smallest* clarifying question OR emit up to 5 self-contained interpretation cards (one-line build preview each; proven on Qwen3-4B, UA-Multi 54.8%→69.4%). Wired into plan-mode. *Kills the silent-guess→build→repair cycle that burned the Zeytin/Volta live tests — the deepest expression of "operate from understood intent."*
- **14.6 Living spec artifacts + traceability** — `requirements.md` (EARS "WHEN…SHALL…") / `design.md` / `tasks.md` under `.nexora/specs/<feature>/`, approval gates + a Quick-Plan escape, **re-read each turn**; per-criterion stable ids + met/unmet/unverified scorecard driving the walkthrough (**fixes the "verified without the wanted line" bug**); read root+nested `AGENTS.md`/`CLAUDE.md` into the hierarchical rules stack.
- **14.7 Engine turbo & instant resume** — **auto-paired speculative decoding** (same-family draft GGUF: Qwen 0.5B→7/14B, Llama-3.2-1B→3.1-8B; "Turbo" toggle + live accept-rate; VRAM-advisor-aware) + **KV-slot cache** (`--slot-save-path` + host-RAM prompt cache; map each session/project to a stable slot, save on idle, restore on reopen — session + `proje-gecmisi.md` + knowledge is the biggest reusable prefix); phase-aware reasoning-token budget.
- **14.8 afterEdit contract + self-healing verify** — auto-detect the repo's OWN format+lint+typecheck+test from `package.json`/`pyproject`, run **scoped to edited paths after every write**, feed non-zero output into the existing `lastBuildError`→fix loop; per-model adaptive edit-format with graceful whole-file downgrade (learned in the KB); a diff-only iteration contract so a small ask can't balloon into a full-file rewrite.
- **14.9 Multimodal reach on running sidecars** *(highest impact-per-effort)* — **OCR/document intake** via the live Qwen-VL sidecar (drag a screenshot/PDF/photo → text or structured Markdown); **local image EDITING** by wiring `localImageService` to `init_image`/`mask_image`/`/v1/images/edits` with a new `[EDIT]` directive (img2img / inpaint / instruction-edit) + **Z-Image Turbo** flagship (4 GB-capable); **whisper.cpp push-to-talk dictation** (multilingual, matches the 10-language UI).
- **14.10 Behavior Prover + Point-and-Steer** *(stretch)* — a CDP-driven behavioral self-test (click / submit / assert-handler-fired / assert-state-survives-reload) feeding the existing repro→auto-fix loop (proves "works", not just "renders"); **click an element in the live preview** → capture a stable locator + `file:line`/JSX span → pin as "← EDIT TARGET" for the next turn (reuses `debugInspect.ts` + comment-to-steer).

**Deliberately CUT (off-mission — logged so we don't drift):** cloud/background coding agents that clone a repo and open PRs on their own infra (Bugbot-fixer, Copilot cloud agent, mobile/Slack agents); cloud one-click deploy + cloud backend provisioning (keep only a **local** "Ship-It" build+serve+export, cloud publish a labeled opt-in far-backlog step); local text-to-video (minutes/clip); Fusion-style tab-autocomplete (XL, needs a custom model — the `[SYMBOL]` + retrieval work is the higher-leverage prerequisite); full parallel multi-agent worktree "Agent Manager" with best-of-N judging (XL, gated by the LLM↔SD VRAM mutex — buy reliability first via 14.4/14.5/14.8); and a **raw user-scriptable shell-hook runner** (reintroduces keyword/exit-code gating against the intent-based constraint — the useful part is reframed as the 14.8 afterEdit contract + local-model-evaluated policy rules).

**Full research, all 10 agents' findings, per-area gaps, and the 9 ranked themes → [`ROADMAP-INTENT-BRAIN.md`](ROADMAP-INTENT-BRAIN.md).**

---

## Phases 15–20 — Session Integrity, Transparency & Context Economy (v0.21+) — PLANNED

> Designed 2026-07-12 from a **7-agent deep study of Piebald AI + the `claude-code-system-prompts` repo** (515+ extracted Claude Code prompt fragments), with an in-code parity check. Verdict: NexoraAI is already *ahead* of Piebald on ~26 axes; only ~10% of Piebald genuinely applies, and Piebald's paywall/cloud-control-plane/cloud-sync/OAuth-bridge/telemetry/web-mode pieces are explicitly **rejected** (incompatible with local-first + free + intent-based). The real leverage is our own architecture maturing + prompt-engineering discipline from the CC repo. Every item below stays 100% on-device and intent-based.

- **Phase 15 — Session Integrity & Config Profiles** *(high · M)* — reboot-resilient **pending-approval serialization** (a `[RUN]`/git-push ask persists across a crash, re-hydrated on relaunch — sibling of the v0.20.1 startup-queue fix); user-facing **config profiles** (`{system-prompt, sampling, directive allow-list, MCP, trust tier}` per session; presets Ideation/Coding/Frontend); sidebar **per-session status badges**.
- **Phase 16 — Radical Transparency (local-first as the feature)** *(high · M)* — **Motor raw-prompt + local-inference inspector** (exact prompt/payload/sampling/response for llama-server AND API → "prove nothing left the machine", turning Piebald's paywalled HTTP-inspector into a positioning pillar); **auditable-by-design directive docs**; local conversation/diff **export to file** (honest substitute for cloud share-links).
- **Phase 17 — Context Economy for Small Local Models** *(high · L)* — **subagent context-offloading** (run `[SEARCH]`/repoMap/scans in an isolated child pass returning only a distilled summary — the biggest lever for 4–8K windows); sequential cheap→expensive reduction pipeline; memory-attach precision + valid ZERO-result; modular prompt assembly + **KV-slot cache breakpoints**.
- **Phase 18 — Prompt-Engineering Discipline** *(high · S, nearly free)* — **U-shaped attention** (bookend LOCAL-FIRST/INTENT/privacy + risky-directive rules); **principle-over-procedure + WHY** in directive docs; **stripped-context two-stage permission classifier** (judge on payload, not the model's own justification); outcome-first chat/prose block; narration-first autonomous state.
- **Phase 19 — Scaling Memory & Self-Managing Automation** *(med · M)* — memory-consolidation **"dream" cycle** over the knowledge base + `proje-gecmisi.md`; **token budget** as a first-class declared constraint; **self-terminating scheduled tasks** (condition-met → auto-remove).
- **Phase 20 — Advanced Session Model & Hardening** *(med · L)* — conversation **branching (DAG)**; **security-review pass** with confidence filter; local **Whisper dictation**; smooth-streaming easing toggle.

**Top-3 first moves:** (1) pending-approval serialization, (2) Motor prompt-inspector, (3) subagent context-offloading. **Full study, 26-item parity check, prompt-engineering wins, and the skip list → [`ROADMAP-PIEBALD.md`](ROADMAP-PIEBALD.md).**

---

**One-sentence summary:** move the engine to llama-server + grammar and auto-verify every generation, and today's 7B results will come from a 3B and 14B results from a 7B — that is what "making small models masters" means; add "open folder" and visual self-review on top, and the app graduates from a site builder to a local coding assistant. **Phase 5 fused every error sense and every repair hand into one Debug Engine — scan, see, locate, fix, verify. Phase 6 gives that engine judgment: compiler eyes, a real debugger's hands, edits that cannot miss, verification that uses the product, and memory that turns every failure into a better next attempt. Phase 7 wraps that engine in the workflow that defines Codex and Antigravity: a live plan, evidence as documents, git-truth review, comment-to-steer, two-layer trust, a visible terminal, a task inbox, and knowledge that survives the session.**
