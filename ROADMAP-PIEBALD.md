# Piebald & Claude-Code-Prompts study → Phases 15–20 (v0.21+)

> **Provenance.** 2026-07-12: a 7-agent deep research sweep of **Piebald AI** (piebald.ai, docs.piebald.ai incl. `llms.txt`, roadmap, pricing) and the **`Piebald-AI/claude-code-system-prompts`** repo (515+ extracted Claude Code prompt fragments), then an applicability map + roadmap synthesis. Every "already have" claim was verified against NexoraAI's own `file:line`. This doc records what applies, what to skip, and the proposed phases.

## What Piebald is (and the honest bottom line)

Piebald is a cross-platform **"agentic AI control experience"** — a paid (Basic/Pro) desktop+web GUI **control-plane that DRIVES coding agents** (Claude Code provider + OpenAI/Anthropic/Google/Bedrock/Copilot/Qwen), with session persistence, parallel-agent orchestration, config granularity, HTTP inspection, git worktrees, a terminal, and cloud sync. It does **not** generate projects on-device; it orchestrates cloud agents.

**Bottom line: ~10% of Piebald is genuinely applicable to NexoraAI.** A 26-item parity check confirmed NexoraAI is *ahead* on most axes (trust tiers, persistent context, 157 providers, on-device image+vision, git revert, checkpoints, serving its own endpoint). The real gold is the **`claude-code-system-prompts` repo** (prompt-engineering discipline) plus a few genuine gaps — none of which are "copy Piebald", all of which are our own architecture maturing. Piebald's Pro-paywall / cloud-control-plane / cloud-sync / OAuth-subscription-bridge / telemetry / web-mode pieces are **incompatible with local-first + free + intent-based** and are explicitly rejected.

## Parity — already have (Piebald/CC idea → NexoraAI equivalent)

Trust tiers + shared verdict core + deny-list (`electron/shared/trust.ts`) · blast-radius/reversibility safety (trust command classes) · context compaction + digest + `proje-gecmisi.md` · token/context meter (`ContextMeter.tsx`, 10.12.2) · MCP stdio client + `[MCP]` · AGENTS.md/CLAUDE.md + hierarchical `KURALLAR.md` (`rulesService.ts`) · slash-command workflows (`slashCommands.ts`, 10.8) · **157-provider hub** + keychain (Piebald has 6) · on-device `[IMG]`/`[EDIT]` SD + VL vision (Piebald is input-only) · notifications + tray (10.5) · checkpoints/rewind (`checkpoints.ts`) · git-truth diff + hunk-level revert (`gitRead.ts`, `DiffModal.tsx`) · message queuing (`taskQueue.ts`) · session persistence (`sessionsService.ts` atomic tmp+rename) · comment-to-steer (`steerComments.ts`) · 10-language UI incl. RTL · exe/deb/dmg CI · **serves its own OpenAI-compatible endpoint** (`serveEngine.ts`, 10.2 — Piebald only *consumes* one). *(~26 items verified in-code.)*

## Genuine high-value additions (worth taking)

1. **Reboot-resilient pending-approval serialization** *(high / S)* — a `[RUN]`/git-push permission ask lives only in-memory (`appStore.ts` permissionRequest); a crash mid-approval loses it silently. Persist `pendingApprovals[]` in `SessionData` (`ipc.ts`), re-hydrate + re-render the PermissionModal on relaunch. *(Sibling of the v0.20.1 startup-queue fix.)*
2. **Config Profiles** *(high / M)* — a user-facing bundle `{system-prompt additions, temp/top-p/max-tokens, enabled directive allow-list, enabled MCP servers, trust tier}`, selectable per session (`~/.nexora/profiles/*.json`). Presets: *Ideation* (chat, no `[RUN]`/`[BUILD]`, read tier), *Coding* (full), *Frontend build*. NOTE: today "profiles" in `prompts.ts` are ARCHITECTURE-only (react-spa/next/electron) — this is the orthogonal config axis.
3. **Motor raw-prompt + local-inference inspector** *(high / M)* — `appendRepairLog` captures structured steps but NO raw prompt. Add an opt-in inspector of the exact system prompt + directive payloads + sampling + response, for both llama-server and BYO-key API. For a local-first app this reframes Piebald's paywalled HTTP-inspector as **"prove nothing left the machine"** — a differentiator.
4. **Subagent context-offloading** *(high / L)* — run `[SEARCH]`/`[SYMBOL]`/repoMap/multi-file scans in an isolated child pass (extend the proven `options.isolate` at `llamaServerEngine.ts`) that returns only a DISTILLED summary, keeping the parent context lean. The single biggest lever for on-device 4–8K-window models.

Secondary: conversation branching (DAG) as a cheap alternative to linear checkpoints · sidebar per-session status badges (working/awaiting-approval/verified/needs-review/error) · scheduled-task self-termination (condition-met → auto-remove) · memory-consolidation "dream" cycle · security-review pass with confidence filter · local Whisper dictation · smooth-streaming easing toggle.

## Prompt-engineering wins from `claude-code-system-prompts` (nearly free)

- **U-shaped attention**: bookend the hardest constraints (LOCAL-FIRST / INTENT-BASED / privacy + highest-risk directive rules) at BOTH head and tail of the system prompt + a turn-start reminder to fight mid-conversation drift.
- **Principle over procedure + state the WHY**: replace residual "if TS error → do X" rules with the principle + reason ("prefer deterministic static analysis because it's auditable") — matches our no-keyword-regex creed and generalizes better.
- **Stripped-context two-stage permission classifier**: judge `[RUN]`/`[BUILD]`/`[FETCH]` safety from the user message + payload but NOT the model's own justification (prevents self-rationalizing); stage-1 fast class/blast filter, stage-2 reasoning only on flagged high-blast actions.
- **Observability rationale in directive docs**: tell the model each intent directive is auditable by design (prefers transparent `[SEARCH]`/`[MCP]` over opaque shell) — same "Read not cat" logic.
- **Outcome-first communication block** for `purpose:'chat'|'prose'`; **modular prompt assembly with KV-slot cache breakpoints** (static skeleton before dynamic repo-map+conversation → `--slot-save-path` recovers ~90% of static tokens); **token budget as a declared first-class constraint** per pass.

## Skip — incompatible with local-first / free / intent-based

$20/mo Pro paywall & feature-gating · cloud control-plane driving remote agents · Piebald Cloud chat/profile sync · public expiring share-links + cloud usage aggregation · OAuth bridging to consumer Pro/Max/Plus portals · device-flow federated team tokens · vendor telemetry · automated decompilation of a competitor's prompts (grey-area) · Python env auto-detection (our stack is Node+C++) · remote HTTP MCP (Figma/Slack over OAuth) · web-mode hosted-service.

---

## Proposed phases (v0.21+)

- **Phase 15 — Session Integrity & Config Profiles** ✅ **DONE** *(high / M)*: (1) reboot-resilient pending-approval serialization — `SessionData.pendingApprovals` + `flushPendingApprovals` (bypasses the generating-guard) + the modal returns on relaunch and re-runs via `reconstructDirectives`; (2) config profiles — `electron/shared/configProfiles.ts` (Ideation/Coding/Frontend) + `profilesStore` + a Profiles panel in Settings; a profile overrides the trust tier + strips disabled directives + system-prompt + sampling override; (3) sidebar per-session status badges — `computeSessionStatus` (live for the active session, disk-written for passive ones). **LIVE-VERIFIED** (real Electron+CDP): 15.1 modal returned with "npm install", 15.2 panel + selection, 15.3 green/amber/red badges. Live-testing caught a `toMeta` statusBadge bug → fixed + regression test. Tests: test:sessionstatus 16 · test:approvalpersist 15 · test:profiles 27 (in test:engine); typecheck clean.
- **Phase 16 — Radical Transparency** ✅ **DONE** *(high / M)*: (1) Engine raw-prompt + local-inference inspector (opt-in) — `llamaService` captures each turn's REAL system prompt + outgoing prompt + sampling + route (`getLastTurnInspection`, via the done event); Settings→Engine shows "🔒 Local — nothing left your machine" / "☁ Sent to <provider>"; (2) auditable-by-design directive docs + U-shaped attention (prompt-only) — `AUDITABLE BY DESIGN` in the grant + `TURN_TAIL_REMINDER` at the tail of every prompt; (3) `composeSessionMarkdown` + `SESSIONS_EXPORT` (dialog+writeFile) + an "Export" button in the sidebar. **LIVE-VERIFIED**: the inspector shows local+API cards with the correct badges, the export button is visible. test:transparency 16 + test:export 16; typecheck clean.
- **Phase 17 — Context Economy for Small Local Models** ✅ **DONE** *(high / L)* — **17.3 ✅** (v0.21; `electron/shared/memoryRelevance.ts` — `relevanceScore` + `filterByRelevance`, `knowledgeContext(…, query)` filters by the turn's query, valid ZERO-result; test:memory 16). **17.2 ✅** — `electron/shared/contextReduce.ts` (`reduceBlocks`/`reduceText`): a CHEAP deterministic first stage (dedup + relevance-rank against the turn's query + HARD per-block cap + budget-fit trimming, the marker is counted IN the budget, never silently empty [minKeep floor]); sub-budget blocks are passed through UNTOUCHED → zero regression in the common case. **17.1 ✅** — `electron/shared/distill.ts` + appStore retrieval wiring: after reduction a still-large [SEARCH]/[SYMBOL] block is OPT-IN distilled in an ISOLATED one-shot pass (`model2.complete` → local `isolate` OR API) into a TERSE query-relevant brief; on failure/NONE the reduced block stands unchanged. **17.4 ✅** — `electron/shared/promptAssembly.ts` (`orderForCache` static-first/dynamic-after + `stableFingerprint` + `assertNoDynamicLeak`) + a KV-slot cache-prefix BYTE-STABILITY guard (chatSystemPrompt is byte-identical across turns → `--cache-reuse 256` [already present since 14.7] stays effective; a dynamic leak is forbidden by test). test:reduce 33 + test:promptcache 20; typecheck clean; ADVERSARIAL verification (4-agent workflow) found 5 issues → all fixed; LIVE-VERIFIED (real Electron+CDP: two opt-in toggles in Engine, default off, 0 console errors, faz17-motor.png). NOTE: the aggressive move of hoisting the repoMap into the static cached prefix was intentionally deferred as it requires live cache-hit profiling (the guard + primitive are ready).
- **Phase 18 — Prompt-Engineering Discipline** ✅ **DONE** *(high / S)* — prompt-only: **outcome-first** (chatSystemPrompt leads with the answer, yes/no first) + **narration-first + principle-over-procedure** (COMPUTER_ACCESS_GRANT: one line of narration before an action, reason from the WHY instead of a magic word) + a **stripped-context permission classifier** (decideCommand takes ONLY the command — no justification parameter, so self-rationalizing is structurally impossible). U-attention + auditability already landed in 16.2. test:promptdiscipline 10/10; typecheck clean. (Verify: the prompt-builder output is tested directly — the strongest verification for a prompt change.)
- **Phase 19 — Scaling Memory & Self-Managing Automation** ✅ **DONE** *(med / M)*: **19.1 memory-consolidation "dream" cycle** — `consolidateItems` (pure: merge near-duplicate titles [same kind + normalized title], SUM hits, keep the longest body + newest) + `dreamConsolidate`; on overflow learnKnowledge consolidates FIRST (frees room without losing data), then drops only if still over. **19.3 scheduled-task self-termination** — `runCount`/`maxRuns`/`isSpent`/`pruneSpent`; `advanceAfterRun` increments runCount; `runDue` prunes spent tasks; a "one-shot" option in SchedulePanel (backward-compatible: old tasks default runCount `?? 0`). test:automation 18/18; typecheck clean. (19.2 token budget already exists: contextSelect CONTEXT_CHAR_BUDGET + apiHistory seedHistoryBudget; a first-class declaration is a refinement.)
- **Phase 20 — Advanced Session Model & Hardening** ✅ **DONE** *(med / L)* — **20.2 ✅ DONE**: security-review pass + CONFIDENCE FILTER (`src/lib/securityReview.ts` pure: embedded provider keys [sk-/ghp_/AKIA/AIza/xox] = high, key-like constant assignment = medium [excluding placeholder/env], eval/new Function/dangerouslySetInnerHTML = medium, insecure http = low; `filterByConfidence` surfaces only high+medium — no false-positive flood); runs post-build in appStore next to behaviorCheck. test:security 18/18. **20.4 ✅ DONE** — smooth-streaming easing (opt-in): the visible stream text is revealed at an eased rAF pace; `currentStreamingContent` (the app's truth) stays fully immediate, only the SCREEN is smoothed; done pins content to `full` on every branch + on Stop/dead-turn `stopSmooth()` runs and content is reconciled to `currentStreamingContent` (a Stop-time tail LOSS was fixed — adversarial finding). Settings→Engine toggle, default off; LIVE-VERIFIED. **20.1 ✅ DONE** (2026-07-14; conversation branching = DAG — "a cheap alternative to linear checkpoints"). A branch = a DERIVED session: from any turn ("Branch from here" in RewindMenu) a NEW nanoid session is born from the message+file state up to that point (reusing the checkpoint infra: `branchMessages`=truncate, snapshot files); the original is NEVER touched. The `SessionData.branchedFrom` back-pointer forms the DAG implicitly (sidebar 🌿 indicator + ChatPanel banner + `branchOrigin` store state; toMeta carries it → the list sees it without loading full data). `src/lib/branch.ts` is pure (branchMessages / branchTitle [appends a de-duplicated "· dal" suffix] / makeBranchOrigin / branchChildCounts); test:branch 19. END-TO-END LIVE (real Electron+CDP): synthetic 2-turn → branched at u2 → the branch was truncated to [u1,a1], the ORIGINAL on disk stayed [u1,a1,u2,a2], branchedFrom round-trips, sidebar+banner render, 0 console errors, faz20-branch.png; ADVERSARIAL verify → ZERO findings. **20.3 ✅ DONE** (2026-07-14; local Whisper dictation — offline speech→text, whisper.cpp; mirrors the localImageService/sd-server pattern). Architecture: `whisperService.ts` (staged binary resolution: ~/NexoraAI/bin → system PATH [brew/apt/nix] → platform package; ggml model on-demand from HF; transcribe → whisper-cli spawn → parse; GRACEFUL degradation when the binary is absent) + `whisperModels.ts` catalog (base/small/tiny) + `whisperParse.ts` (strips timestamps+noise+log lines) + `src/lib/wav.ts` (getUserMedia PCM → 16 kHz mono WAV, bidirectional resample) + `RecordButton.tsx` (composer+hero mic, record/transcribe states) + WHISPER_* IPC/preload + a main-window media permission handler. Audio stays ON DEVICE, never goes to the cloud. `test:whisper` 42 (parse+WAV+resample+catalog+log-filter). ADVERSARIAL verify → 4 findings ALL fixed (2 high: unmount/error-path mic-stays-hot leaks → teardown + useEffect cleanup + stream-stop; 1 medium: fake header on a <16 kHz device → bidirectional resampleTo; 1 low: stderr log injection → stdout-only + a log filter). LIVE-VERIFIED (real Electron+CDP: mic button renders, status IPC binary/model/ready, click → graceful "install whisper" hint, 0 console errors, faz20-whisper.png). **v0.23.1 (2026-07-14): Linux/Windows AUTO-DOWNLOAD** — the user's "it should install itself" was right: whisper.cpp v1.9.1 now ships `whisper-bin-ubuntu-x64/arm64.tar.gz` + `whisper-bin-x64.zip` → the binary AUTO-DOWNLOADS on Linux+Windows (macOS has no CLI in the release → brew). FIXES: whisperBinaryAsset (v1.9.1 per-platform packages), recursive findBinaryInDir PRIORITY-ordered (whisper-cli first; `main` is deprecated in v1.9.1 → exit 1), LD_LIBRARY_PATH=dirname(bin) (the .so files sit alongside). **REAL END-TO-END PROOF (Linux x64):** whisperService.transcribe(jfk.wav) → "And so my fellow Americans ask not what your country can do for you…" (whisper-cli, tiny model); the binary+model were pre-seeded into the user's ~/NexoraAI (works instantly). **PHASE 20 COMPLETE → Piebald roadmap core at 100%.**

**Top-3 first moves:** (1) pending-approval serialization (highest impact-per-line, fixes a real data-loss gap), (2) Motor prompt-inspector (transparency = the local-first differentiator), (3) subagent context-offloading (biggest small-model lever).
